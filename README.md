# ZeroCraft — 引导分支（bootstrap）

本分支只含会话引导最小集：CLAUDE.md（项目规矩）+ .claude/ 启动钩子（开机自动切换到真实工作分支）。

**全部真实内容在 `claude/mainbranch`**：`git fetch origin claude/mainbranch && git checkout claude/mainbranch`

稳定版快照由 owner 点名后同步至此。
