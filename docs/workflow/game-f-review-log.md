# Game F 策划代码审查日志（PF Review Log）

> 审查人：策划 PF（用户 2026-06-10 授权代码审查权）。节奏：~3 分钟轮询 `claude/mainbranch` 新提交。
> 审什么（checklist，对每个 game-f 相关提交）：
> 1. **流转符合 `game-f-flow-spec.md`**：状态机结构（§3.2/§3.3）、全局 id 先登记 §3.1 再用、数值出自 §4 表、开发次序守 §6.2 队列（勿抢跑 Phase 2/3）。
> 2. **宪法（manifesto）**：`src/games/game-f/` 零 system/零手写战斗 UI（只有数据装配）；引擎改动必须挂 requests.md 已 greenlit 的 REQ、带验收测试。
> 3. **回驳清单不复活**（auto-chess §7 + REQ-023 YAGNI）：草船借箭/调虎离山/每帧羁绊/PvP 等别悄悄捡回。
> 4. **全绿声明属实**：抽测 `npx vitest run`。
> 5. **文档同步**：完成项在 inbox 回执、flow-spec §6.1/§6.2 刷新、新 id 进 §3.1。
> 裁决分级：✅ 符合 ｜ ⚠️ 偏差已声明/已入队列（追踪不拦截）｜ ❌ 必须修复（同时在 inbox 对应任务行加「策划审查批注」）。

---

## 第 1 轮 · 2026-06-10 · 范围 `09b6059..9a88db1`（7 个提交）

**结论：无 ❌。REQ-F-032/033 引擎件 + F-5/F-6/F-7 接线全部符合设计案；4 项 ⚠️ 偏差均已自我声明且在队列内。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `d863f92` | 引擎 REQ-F-032（Caster/SpawnRequest.overrides 透传 + Effect destroy-tagged） | ✅ 走了正规 REQ 流程（证伪重组→入池→落地带 4 验收测）；"零新系统、三处收窄扩展"符合下沉纪律；语义与 flow-spec §3.3 prep⑥/resolution 清场吻合 |
| `46f78cc`+`5ca52ec` | REQ-F-033（'@local:' 模板内部引用重映射）提报+落地 | ✅ 接入暴露真缺口→先提池再实现，流程教科书级；"复合预制整体生灭"正是 §3.3 棋子实例语义所需 |
| `d56b87f` | F-5 血条/蓝条 + F-6 八阵图冰冻 接线 | ✅ 按 inbox 处方原样落地（hp fromParent / mp 全局唯一 / 条无碰撞组件不参战不计 Zone）；测 +2 |
| `88b3107` | F-7 回合重置 + 多回合循环 | ✅ 符合 §3.3：prep⟲combat⟲resolution 循环、in_combat 门控保留、deploy/wipe 走"臂 Flag→EventWhen(edge)→信号"单拍纪律（置位/复位成对）、槽位持久（无 Tag 防误清）、敌槽按 `deploy_stage_N` 分流给关卡表留口。**§3.1 id 注册表已补登 5 个新 id（回执属实）**；§5/§6.1/§6.2 同步刷新，文档纪律好。测 9/9 含两回合循环断言（清场无孤儿/槽位库持久/新实例满状态） |
| `4b18290`+`9a88db1` | 资源库重构（assets/studio） | ➖ 非 game-f 设计案范围（另一条线），本日志跳过 |

**⚠️ 追踪项（已声明、合队列，不拦截）**：
1. `ready` 开战未接——prep 仍 `after 40` 自动开战（§2 裁决"玩家点开战"；队列 P2 输入域）。
2. L1 `run_flow` 未接（§3.2：stage/round 指针、victory/defeat、round_done 握手）——P0 余项；接上后 resolution 出口须由"60 拍直回 prep"改为 done 握手（PE-F 已在注释自知）。
3. 经济仍是 prep 固定 +5 金占位（§4.1 收入爬坡/利息/连胜金 = P1）。
4. 玩家伤害仍固定 -5（§4.2 公式 = P1）。

**验证**：`vitest` 142 文件 / **1009 全过**（高于回执声称的 967——并行线又有新增）；game-f 实测 9/9。

---

## 第 2 轮 · 2026-06-10 · 范围 `a5a9ad7..0d8541e`（1 个提交）

**结论：✅ 符合。无 ❌。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `0d8541e` | 用户实测三连修：① MANA_FILL 50→20（蓝条频闪=节奏数据非 bug，探针证据：1.5s 整循环×8 子异步=频闪+诸葛近永冻；改后 5 攻一大招，CC 有窗口） ② 名牌势力色→队伍色（两边各一个吴=绿，分不清谁打谁；用户拍板；势力 tint 保留待羁绊期徽记） ③ 瞬移做了证伪重组（双 writer 打架/tween 追不了动态格点/回 steering 丢 A*）→ 正确提 REQ-F-034 入池待主程，**没有在游戏层 workaround** | ✅ 纯数据调参 + 纪律范本（缺口提池不硬绕） |

**⚠️ 追踪项**：
5. `art-data.md` 的"势力色由名字 Text 承担"已与实现脱节（名牌现为队伍色）——文档漂移，待 art-data 修订（归我/PE-F 均可）。
6. Phase 3 羁绊上线时需补势力辨识通道（徽记/描边，tint 数据已留）。

**验证**：`vitest` 142 文件 / 1009 全过。第 1 轮追踪项 1–4 未动（在队列内，正常）。

---

## 第 3 轮 · 2026-06-10 · 范围 `e6fd94f..ba26265`（1 个提交）

| 提交 | 内容 | 裁决 |
|---|---|---|
| `ba26265` | artlib-tags（资源库标签修正/补充） | ➖ 非 game-f 设计案范围，跳过；套件守门 1009 全过 |

---

## 第 4 轮 · 2026-06-10 · 范围 `ba26265..e3f3b0d`（1 个提交）

**结论：✅ 符合。无 ❌。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `e3f3b0d` | 引擎 REQ-F-034 落地：`GridMover.glideSpeed`（逻辑格瞬步=HexPos 仍是 SIM 真相进 hash，Transform 恒速逼近+精确贴齐；缺省不设=原瞬移零迁移） | ✅ REQ 流程完整（open→裁决 A/回驳 B 带理由→done）；确定性纪律守住（Transform 不被 Condition 读、sqrt 走项目既有裁定先例）；与 F-030 冻结语义一致（时间静止视觉同停）；+4 验收测；inbox 派 F-8 + handoff 同步刷新 |

**验证**：`vitest` 1013 全过（+4）。待办观察：F-8（棋子接 glideSpeed，1 字段）pending 中，下轮关注 PE-F 取值与 MOVE_PERIOD=48 的匹配——相邻格 ~33px / 48 tick ≈ 0.7 px/tick，取值低于此则滑行追不上逻辑步、滞后逐格累积（不回退但越拖越远），建议 ≥0.8。

---

## 第 5 轮 · 2026-06-10 · 范围补审 `2882943`（推送窗口插入的 1 个提交）

| 提交 | 内容 | 裁决 |
|---|---|---|
| `2882943` | assets/assembly：语义标签上图 + AI 选材 `art:` 确定性解析 | ➖ 非 game-f 线，跳过（注：`art:<关键词>` manifest 引用是游戏数据未来可用的资产寻址形态，与宪法"数据驱动选材"方向一致）；套件守门 1022 全过 |

---

## 第 6 轮 · 2026-06-10 · 范围 `e3f3b0d..9b8587c`（1 个提交，MVP-1 第一批）

**结论：✅ 高度符合（自报"照 flow-spec §3.2/§4.1/§4.2/§4.5"，逐条核实属实）。无 ❌。第 1 轮追踪项 2/3/4 就此销账。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `9b8587c` | L1 run_flow（boot→round→advance→victory/defeat，round_done 握手，banded 进位）+ L2 接 done 握手态（替直回 prep，正是第 1 轮预告的改法）+ 经济三件套出 flow 进表（收入爬坡 2,2,3,4,≥5 / 利息 5 档 / 连胜金 3 档，armed 窗纪律与 deploy/wipe 同构）+ 败方伤害=阶段基础+存活近似 2（§6.2 P1 允许的近似，REQ-022 真值 Phase 3 换）+ 关卡表前 2 阶段（敌阵=数据，hpMul 缩放，deploy_stage_N 分流） | ✅ §3.1 注册表先登记后用（核实属实）；victory 阈值参数化 STAGE_COUNT=2（表扩 5 阶段时改常量+数据即可，注释自知）；测 9→10 |

**📌 设计稿勘误（归策划，第 7 项追踪）**：`game-f-auto-chess.md` §4.4 利息草案"5 档同发同名 `give_interest` → +1×命中档数"与引擎信号**按名集合**语义不符（同名同拍塌成一发）。PE-F 实现用**每带唯一信号名**（`interest_*` 族）是正确修正。设计稿该节需补注，防后来者照抄草案。

**⚠️ 追踪项更新**：✅ 销账 2（L1 run_flow）、3（经济）、4（伤害公式）；存量：1（ready 开战，inbox 归口主程输入域）、5（art-data 色彩漂移）、6（Phase 3 势力辨识）、7（§4.4 勘误，本轮新增）；F-8 取值观察继续。余 MVP-1：商店 P0、等级/经验 P2。

**验证**：`vitest` 143 文件 / 1023 全过；game-f 10/10。

---

## 第 7 轮 · 2026-06-10 · 范围 `9b8587c..409728c`（1 个提交，rebase 窗口并入补审）

**结论：✅ 符合。瞬移三 bug 全闭环。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `409728c` | F-8 接入：棋子模板 GridMover 加 `glideSpeed:0.8`——**取值正是第 4 轮审查建议的下限**（≥0.7 留余量），+1 测试断言"每拍位移≤0.8、真在滑"；顺手修了 art-data 色彩漂移（追踪项 5，PE-F 自销） | ✅ 纯数据 1 字段；审查→接入闭环范本 |

**⚠️ 追踪项更新**：✅ 销账 5（art-data 漂移，PE-F 自修）、F-8 取值观察（0.8 达标）。存量：1（ready——本轮已回驳"归主程"，批注见 inbox F-7，改归 PE-F 纯数据接）、6（Phase 3 势力徽记）、7（§4.4 勘误注已落，闭）。余 MVP-1：商店 P0、等级/经验 P2。

**验证**：`vitest` 143 文件 / 1024 全过。

---

## 第 8 轮 · 2026-06-10 · 范围 `409728c..d6a1991`（1 个提交）

| 提交 | 内容 | 裁决 |
|---|---|---|
| `d6a1991` | artlib-tags jellyfish 错标修正 + `scripts/contact-sheet.mjs` 拼图审计工具 | ➖ 非 game-f 线，跳过；套件守门 1024 全过 |

---

## 第 9 轮 · 2026-06-10 · 范围补审 `299b498`（推送窗口插入）—— **本轮有 ❌（预防性拦截）**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `299b498` | 引擎 REQ-021 扩展：self-rule 加 `spawn` 动作（self 轴的 caster 对偶，解唯一 id 脚手架，通向合体）+ 新派 inbox F-5★ 普攻链迁移任务 | 引擎件本身 ✅（缺口论证扎实、9 测、零游戏代码）；**配套迁移处方 ❌**（见下） |

**❌ 拦截（处方层面，代码未犯）**：F-5★ 处方称"in_combat 门控可删、改用目标存在性"——亲核证伪：
① deploy 在 prep 展开（§3.3 prep ⑥）、aggro 无门即锁敌 → 目标门备战期失效，照做会**备战期开打 + 结算期幸存者互殴**，违反 §3.3/§1.2 铁律（备战不打是金铲铲定义性语义）；
② `evaluateSelfCondition` 只读自身组件、无全局回退 → SelfRule 表达不了 `timer ∧ 全局 in_combat`；逐单位发 flag 副本=Gap C 禁区。
**处置**：inbox F-5★ 已加 ❌ 批注（先修处方再动工）；**提 REQ-F-035**（`SelfRule.whenGlobal?` 全局阶段门，最小一格，普适于"实体自治受全局相位约束"形状）交主程；**大招半边不阻塞可先迁**（蓝由有门的普攻攒，备战 `mp≥100` 不可能成立）；验收补「备战/结算期无伤害事件」防回归断言。

**验证**：`vitest` 143 文件 / 1028 全过（self-rule +4 测）。

---

## 第 10 轮 · 2026-06-10 · 范围 `c2f69ad..53066da`（2 个提交）

| 提交 | 内容 | 裁决 |
|---|---|---|
| `f382e55` | assets/apollo：入库主动扫描自动打标管线 | ➖ 非 game-f 线，跳过 |
| `53066da` | apollo.py 就绪探测 127.0.0.1 修复 | ➖ 非 game-f 线（启动工具），跳过 |

套件守门 1028 全过。game-f 挂账无变化（REQ-F-035 待主程、商店 P0 / F-9 大招半边 / ready 待 PE-F）。

---

## 第 11 轮 · 2026-06-10 · 范围 `53066da..1e875ef`（1 个提交）

**结论：✅ 符合——第 9 轮拦截的完整闭环（拦截→REQ-F-035→零修正落地→F-9 解锁）。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `1e875ef` | 引擎 REQ-F-035 落地：`SelfRule.whenGlobal?`（全局求值 ∧ self when，先求短路；once 门关期 armed 保持）；**采信第 9 轮拦截判定**、修正 299b498 的目标门处方；附带排掉 F-9 必踩的定序雷（self-rule runsAfter flow/resource-apply/zone-occupancy/group-count + 守护测"开/关门当拍生效"）；治理固化进 CLAUDE.md（**引擎只归主程**、PE/策划走 requests.md；**rebase 带进新提交必须重跑全套**——后者正是本审查循环第 5/9 轮实际执行的纪律，现成文） | ✅ +3 测；F-9 的 Lead 解锁批注处方正确（普攻/大招一律 whenGlobal in_combat）；我的 ❌ 批注已标"已解决"防误读 |

**验证**：`vitest` 143 文件 / 1031 全过（与自报一致）。F-9 现已无阻塞，待 PE-F 迁移（验收含"备战/结算期无伤害"断言 + 2×同模板不串台）。挂账余：商店 P0、ready、等级/经验 P2。

---

## 第 12 轮 · 2026-06-10 · 范围 `6868aa8..0dc786a`（6 个提交）

**结论：无 ❌。F-9 实测撞墙→三连提池→主程响应的流水线全程合规；ready 走策划批注路线落地（闭环 #3）。**

| 提交 | 内容 | 裁决 |
|---|---|---|
| `f00425f`+`7330bd0` | PE-F F-9 实测拦截：REQ-F-036 拓扑环（守护测漏 caster/prefab/hitbox 链，复测重开附走向推演）+ F-037→039 大招三堵墙 + F-038→040 商店牌码，三连提主程；**实测不过即回退保持全绿** | ✅ "发现引擎不够→提池不 hack"纪律范本 |
| `f74b58a` | 主程 REQ-F-036 二刷：self-rule 补 `runsAfter:['hitbox']`（决策坐结算链尾），15 能力全家桶守护测 | ✅ 环真断净（复测口径升级） |
| `20ee3e8`+`0dc786a` | 主程 REQ-F-037 odd-r 拓扑同构棋盘（外审 Q5 裁决 c）+ PE-F F-10 迁移（视觉像素恒等、摆子数据保 (col,row)、12/12 测） | ✅ 撞号让位规则正确执行（我方 037/038→039/040） |
| `0360e00` | PE-F 接 ready 开战：clickable→`ready_btn`→set-flag——**正是第 6 轮策划批注的回驳路线**，flow-spec P2 行销账 + §3.1 登记 `ready_btn` | ✅ 闭环 #3（回驳"归主程"→PE-F 纯数据自接成功） |

**REQ-F-039/040 策划立场**：039（一实体一 SelfRule 装不下回蓝+放招）= Phase 2 才真撞上，**同意不阻塞 F-9 普攻半边**；建议 (A) rules 数组（通用"多条自治规则"形状）优先。040（牌码分发）= **商店 P0 唯一缺口、强烈背书 A1+A2**（A2 的"买不起也丢牌"时序硬伤必须一起修，否则商店违反准则的原子购买语义）。

**验证**：`vitest` 143 文件 / 1037 全过；game-f 12/12（odd-r 迁移后含 hash/走位/AOE 回归）。
