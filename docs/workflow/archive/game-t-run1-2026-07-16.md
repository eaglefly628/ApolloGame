# game-t《墨消》· 首轮（run1）档案（2026-07-16 清库·owner 重跑令）

> owner 拍板：首轮产物全清，同一策划案开全新 GD-T/PE-T session 重做（clean-room 验证补牙后的生产流程）。
> **全部产物可从 git 历史找回**：GD 关卡表/仿真 `fd71a2ea`、PE 骨架装配 `6142237d`、正式表接入 `3e4b6d66`（清库提交的父链上）。

## 首轮做到哪

- S1-S4 机器门绿（走查 11 测/骨架/胜负链 t3-flow/30 关表+balance-sim 200 seeds 29/30 带内）；S5-S8 未进入。
- Lead 验收判 NEEDS WORK（台账未生成/评分卡未打/复查门人门全空）→ 触发流程复盘。

## 首轮的价值（留下的不是产物是制度）

1. **四洞四牙**：自基线（审计工具邀请式提示语=事故根因）→ REQ-AUDIT-守门 A；完成自报 → 完成口径铁律（板全绿才叫完成）；无板上架 → 注册即有板守卫；悄悄跳关 → 阶段顺序闸。全部落地并实弹验收（`80b9e706`/`93c56487`/`4ba42089`）。
2. **引擎沉淀**（保留·非 game-t 资产）：match3-board 二期、t2-match3-drag-swap、mount-host helper、t3-flow 消费先例、REQ-M3-计分倍率/三期⑤ settled Flag 两单 spec。
3. **过程记录**：首轮工单 T-001..T-005 全文见本文件同批清库提交的父版本 `docs/design/game-t/requests.md`（git 历史）。

## 新一轮输入（唯一真相）

`docs/design/game-t/{gdd,capability-plan,level-schema}.md`（原封保留）+ 标准启动词模板（`docs/roles/index.md`·含切分支+完成口径）。
