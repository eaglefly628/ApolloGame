# Skill 行为 Spec · <技能名>（模板·复制起卡）

> 对象：`.claude/skills/<name>/SKILL.md` · 类型：技能 · 起卡日期：YYYY-MM-DD
> 移植注：源自 CCGS skill-test-spec 七段骨架，ZeroCraft 化（判词闭集+退出码·无交互审批）。

## Summary（这件零件做什么）

一段话：输入什么、走什么流程、产出什么、判词闭集是哪几个 token、什么时候必须停。

## 静态断言（结构·不需 fixture）

- [ ] frontmatter 含 `name`、`description`（触发条件写清「凡…→用它」）
- [ ] 有明确的阶段/步骤结构（≥2 节）
- [ ] 判词属闭集（如 PASS / CONCERNS / FAIL），无散文式结论
- [ ] 危险/越域操作有域约束声明（写哪、绝不写哪）
- [ ] 有下一步交接（完成后指向哪个仪式/手册）

## 测例（5 原型·失败路径必测）

### Case 1 · Happy Path
**Fixture**：<最小可复现前置> · **输入**：<调用> ·
**期望**：1. … 2. …（编号步骤·可观察行为）
**断言**：- [ ] … - [ ] 判词=<token>

### Case 2 · 失败路径（前置缺失 fail-fast）
前置不满足（缺文件/缺配置）→ 立即停 + 指路补救命令；**不半跑、不产残果**。

### Case 3 · 幂等重入（目标已存在）
产物已在 → 验证而非重建；只补缺口，不覆盖已有。

### Case 4 · 边界（本零件最容易糊的场景）
<如：零输入出空表不报错 / 超大输入截断有声明 / 特殊字符路径>

### Case 5 · 判词降级（部分过）
部分检查不过 → 判词降档（PASS WITH WARNINGS / CONCERNS），缺口**列名字**不笼统。

## Protocol Compliance

- [ ] 域边界：只写声明过的路径；域外→提 requests.md
- [ ] 判词闭集 + 理由带 file:line/实数
- [ ] 无法机验的项标 MANUAL CHECK 交人，不自判通过

## Coverage Notes（诚实声明没测什么）

- <未覆盖场景一句一条；没有就写"无已知缺口"。>
