# Game F 牌组详规(首批)·「以战养战」/「白衣渡江」—— 可实现颗粒度

> 主策划 ｜ 2026-06-13 ｜ 展开 `game-f-cards-and-decks.md` §6 的第 7、第 10 套(**本作独有的两套**)。
> 目的:把"卡牌到底长什么样、要哪些引擎算子"具象化,为 **D0(核对 Game E joker 算子)** 提供靶子。
> ⛔ 尺子:每张卡 = 一组 `{触发条件, 效果}` 算子,落到引擎现成 capability;落不下去的 → **标缺口、列候选 REQ**(交主程评估,先查 Game E 是否已覆盖再决定下沉)。
>
> **卡牌三态**(承 §4.1):**Build**=局外持久带入 ｜ **Run**=局内 Boss 宝箱掉落、本局生效 ｜ **星球**=升级某流派档位(局外持久)。
> 数值均为**首版待平衡**。

---

## 牌组 7 ·「以战养战」(降将·混血 Recruit)

**身份**:把"PvE 敌人(太阁守军)变成你的牌库"。越收编越强,终盘滚雪球。围绕**降将机制**(`game-f-coop-sunliu.md` §2.3:`Mortal.dropTemplate` → 主公拾取 → 入席)。

**核心循环**:补刀太阁 → 掉招降标记 → 拾取入席 → `recruit_count++` → 经济/强化滚雪球 → 收编更多。

| 卡名 | 态 | 效果(首版) | 数据形(capability 映射) | 复用/缺口 |
|---|---|---|---|---|
| **招降令** ⭐钥匙 | Build | 收编的太阁入席时,使用**强化变体模板**(+1 星基础 / +15% hp·atk) | recruit_token 消费 → 装配层据码展开"强化变体" + spawn 时 **self-scope 一次性 buff**(REQ-021) | ✅复用 |
| **以战养战** | Build | 本局每收编 N 个太阁,结算追加金币(滚经济) | `recruit_count` 资源累加 → **banded** EventWhen(≥3/≥6/≥9)→ Effect modify `gold` | ✅复用(Game E banded) |
| **质子为质** | Run | 收编的太阁阵亡时,有几率返还半个招降标记(再收编链) | `mortal.dropTemplate` 概率返还 token | ✅复用 |
| **以夷制夷** | Build | 收编的太阁保留其"看家本领"(信长·天下布武 等)为你所用 | 太阁 Prefab 自带 `Caster` 技能;收编 = 重写 owner/队伍位后技能天然归你队 | ⚠️复用**但需核**:收编后 owner/`TEAM` 位重写 + 信号 self 寻址正确(REQ-021 域) |
| **先登陷阵** | Run | 你的单位 +X attack(帮你抢太阁人头) | per-unit / self-scope flat buff | ✅复用(简单版) |
| **养寇自重** | Build | 不立刻清场、留残血太阁滚利息,但承担反扑风险 | 玩法层选择:关卡数据 + 经济,无新算子 | ✅复用 |
| **降众归心** | 星球 | 太阁招降掉率全档 +1(更易收编) | 改掉落表权重(全局数值) | ✅复用(数值) |

**附魔示例**:`招降令·多彩` 收编强化 +15%→+30% ｜ `以战养战·镀金` banded 金币翻倍。
**围绕**:太阁全谱(信长/秀吉/家康/武田/上杉/真田…)+ 三国低费过渡阵把你撑到能补刀。
**克制关系**:**克**"密集低星杂兵"型守军(可收编池大、补刀容易);**被克**于"少量高星精英 Boss"型守军(可收编目标少、精英难补刀,雪球转不动)。

> **缺口小结**:几乎全复用——这印证「以战养战」是**纯数据可落地的好流派**。唯一进阶缺口是"对满血/特定状态目标加伤"(先登的进阶版),与下面「白衣渡江」同根,见末尾候选 REQ。

---

## 牌组 10 ·「白衣渡江」(刺客·斩首 Last-hit)

**身份**:刺客绕后秒残血 + **抢 Boss 最后一刀(贡献度)**。围绕吕蒙/甘宁 × 刺客职业。**这套是"贡献度=抢人头"机制的可构筑化身。**

**核心循环**:绕后扑残血 → 斩杀线触发处决 → 抢 last-hit → 吃高额贡献 + 收割。

| 卡名 | 态 | 效果(首版) | 数据形(capability 映射) | 复用/缺口 |
|---|---|---|---|---|
| **白衣** ⭐钥匙 | Build | 对生命 <30% 的目标,伤害 ×N(斩杀线) | hitbox 需**读目标当前 hp 比例做条件** | 🔴**缺口**:hp-条件伤害/处决算子 → 候选 **REQ-F-061** |
| **绕后奇袭** | Build | 刺客索敌改为"最高费/最远"敌方单位(直插后排) | `aggro` 现为 `nearestByTag`,需**索敌策略可选**(nearest→farthest/highest-stat) | 🔴**缺口**:aggro 索敌策略 → 候选 **REQ-F-062** |
| **断粮** | Build | 你的单位**补刀(last-hit)**太阁时,该太阁贡献额外 +X、掉金 | 依赖贡献度系统的 **kill-attribution(补刀归属)** | 🟠**系统特性**:贡献系统须钉"谁补刀谁得分"(非战斗算子,系统设计定) |
| **攻心** | Run | 对已损失生命越多的目标,伤害递增 | hp-条件伤害族,同钥匙牌缺口 | 🔴**缺口**:同 REQ-F-061 |
| **锦帆**(甘宁) | Run | 刺客攻速 +X、暴击 +X | per-unit/self buff + `Timer.duration` 调 | ⚠️复用**但需核**:暴击原语是否已存在(无则小缺口) |
| **潜行** | Run | 刺客开局 X 秒不被索敌 | status flag + 敌方 `Perception` 忽略带该 status 的单位 | ⚠️**待核**:能否用 `requireMask` 取反重组?不能则小缺口 |
| **渡江** | 星球 | 斩杀线 30%→提升档位(35%/40%) | 改钥匙牌阈值(数值) | ✅复用(数值),依赖 REQ-F-061 先落 |

**附魔示例**:`白衣·多彩` 斩杀倍率 ×N 提升 ｜ `锦帆·镀金` 暴击翻倍。
**围绕**:吕蒙/甘宁/马超等刺客 × 刺客职业羁绊(`group-count` REQ-022)。
**克制关系**:**克**"高血厚甲单核 / Boss"(斩杀无视厚血、专抢人头);**被克**于"群体铺场低血海"(刺客少、容易被海淹)+ "护盾/治疗回血"(把目标拉出斩杀线、处决落空)。

> **缺口小结**:「白衣渡江」**故意**压在缺口上——这正是价值:它证明"抢人头贡献"这个差异化爽点,需要真实引擎支持(处决 + 补刀归属),不是纯数据能糊弄的。深挖它=提前暴露了 D0 之后该立的 REQ。

---

## 本批暴露的引擎缺口候选(交主程评估 → D0 核对 Game E 后再入 `requests.md`)

> 纪律:**先查 Game E joker(`game-e-joker-roguelike.md`)是否已实现等价算子**,已覆盖则回驳;真缺口才下沉。下列是"待 D0 核对"的候选,不是既定 REQ。

| 候选 | 名称 | 用途 | 通用性(为什么值得下沉) | 风险 |
|---|---|---|---|---|
| **REQ-F-061** | hp-条件伤害 / 处决 | 白衣·攻心·渡江;先登进阶 | "按目标当前生命比例改伤害/触发处决"是**普适**战斗词汇(斩杀/狂暴/残血加伤,所有 ARPG 通用),非一卡专用 | 中:碰 §7 曾回驳的"伤害重定向"边界,要设计成**只读 hp 比例的条件 gate**、不引入伤害分型,范围才窄 |
| **REQ-F-062** | aggro 索敌策略 | 绕后奇袭(直插后排) | 索敌"最近/最远/最高某属性"是**普适**词汇(刺客/狙击/嘲讽都要),一个 enum 参数化现有 `nearestByTag` | 低:`aggro` 已选最近,加策略枚举即可;确定性不变 |
| **贡献系统特性** | kill-attribution(补刀归属) | 断粮;整套贡献度机制 | 贡献度=名次=胜负,**核心系统**;补刀归属是其底层(谁的伤害造成击杀 → 记谁的分) | 中:需在系统设计钉死(伤害来源 `Signal.source` 已有 → 击杀时读 source 记分,REQ-021 域内) |
| 待核·暴击 | 暴击原语 | 锦帆 | 通用 | 低:有则复用,无则小算子 |
| 待核·Perception 排除 | 按 status 屏蔽索敌 | 潜行 | 通用(隐身/潜行) | 低:可能 `requireMask` 取反可重组 |
| 既定·Gap C | 全队光环 fan-out | 任何"全队增益"卡 | —— | 守 §7 纪律:优先"全局 stat + 能力读全局"重组,不为此提前下沉 `group-effect` |

---

> 复诵:「以战养战」≈ 纯数据,验证了卡牌系统的下限;「白衣渡江」压在缺口上,暴露了"抢人头"差异化的真实引擎成本。
---

## D0 核对结论(2026-06-13,已对真实代码 + Game E joker + requests.md)

> 纪律执行:**能组合/已覆盖→回驳;真缺口→才下沉。** 5 个候选过完尺子的结果:

| 候选 | 裁决 | 依据 / 数据写法 |
|---|---|---|
| joker 架构(trigger→when→{op,target,value}) | ✅**已覆盖·回驳** | Game E `jokers.ts` 已证;引擎原语 REQ-011/012(op:mul/order)/013(valueFrom)/014(逐张) 全 **done**。卡牌效果直接照搬此架构 |
| 连携/职业计数(桃园/魏骑/刺客羁绊) | ✅**已覆盖·回驳** | REQ-022 `group-count` **done**(开战锁存计数→越阈值信号) |
| 经济(屯田/以战养战 banded) | ✅**已覆盖·回驳** | banded `EventWhen→Effect` + REQ-013 `valueFrom`,Game E 已证 |
| 招降/重复棋子(招降令 spawn) | ✅**已覆盖·回驳** | REQ-021 `self-rule` + `spawn` 动作 **done**(@299b498) |
| 星球牌(档位升级 ×/+) | ✅**已覆盖·回驳** | REQ-012 `Effect.op:mul/set` + `order` **done** |
| **全队统一 buff**(虎豹骑令/羁绊光环) | ✅**重组·回驳** | 全局 buff 资源 + hitbox `scaleByResource`(REQ-F-047 **done**)。**无需逐单位 fan-out**(守 REQ-023 既裁) |
| **per-unit 异质 buff**(先登/单核喂张飞) | 🟡**MVP 重组·不 greenlit** | 模板变体(强化 strike 模板,纯数据)绕过。`Stats` 虽存在但**全项目无人消费**、hitbox 不读活系数 —— **re-pull 记账,守 YAGNI 暂不下沉**(同 REQ-023 域) |
| 暴击(锦帆) | 🟡**MVP 缓做** | `Stats` 有 crit 字段但 hitbox 不消费;非 MVP 必需,待真实拉动 |
| **hp-条件伤害/处决**(白衣/攻心/渡江) | 🔴**真缺口 → 已提 REQ-F-061** | hitbox 仅 `targetMask`/`requireMask`(Status),无血量条件门 |
| **aggro 索敌策略**(绕后) | 🔴**真缺口 → 已提 REQ-F-062** | `aggro` 写死 `nearestByTag`,`Perception` 无策略字段 |
| 贡献度 kill-attribution(断粮) | 🟠**系统设计项·非引擎缺口** | 复用 `Signal.source`(REQ-021 域,F-058 批已用 `@signal-source`);贡献系统设计时钉"谁补刀谁得分" |

> **D0 一句话**:5 个候选只有 **2 个真缺口(REQ-F-061 hp条件伤害 / REQ-F-062 aggro策略)** 值得下沉、已入 `requests.md`;其余**全被现成能力覆盖或可数据重组**。**卡牌系统的下限是纯数据可落地的**——印证立项判断。per-unit 异质 buff 与暴击守 YAGNI 缓做。

---

## 其余 8 套牌组(同颗粒度,承上文数据词汇 + D0 结论)

> 标注沿用:✅复用 ｜ 🟡MVP 重组(per-unit/缓做) ｜ 🔴撞已提缺口。**注意大多数卡是 ✅** —— 这正是"纯数据下限"的证据。

### 1 ·「虎豹铁骑」(魏·速攻 Aggro)— D1 单机原型首选(最简基线)
身份:魏骑连携滚雪球,序盘压制吃贡献。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **虎豹骑令** ⭐ | Build | 每有 1 魏骑·全骑 +攻 | `group-count` 数魏骑 → 写全局 buff 资源 → hitbox `scaleByResource` | ✅复用 |
| 速攻令 | Build | 前 3 回合伤害 + | flow round 计数 + banded → 全局 buff | ✅复用 |
| 募兵 | Build | 商店魏国权重 + | 预配权重牌袋 | ✅复用 |
| 铁骑突阵 | Run | 魏骑贴脸更快(steering speed +) | per-unit → 强化模板变体 | 🟡重组 |
| 星球·魏 | 星球 | 魏羁绊全档 +1 | `group-count` 阈值 / REQ-012 | ✅复用 |

**克制**:克慢热经济(屯田);被控制(卧龙)+ 高血墙克。

### 2 ·「桃园结义」(蜀·连携 Combo)
身份:刘关张三核同场质变。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **桃园三义** ⭐ | Build | 刘关张同场 → 全属性飙升 | `group-count` 数"桃园"tag ≥3 → 全局 buff | ✅复用 |
| 三顾茅庐 | Build | 找特定武将(诸葛)概率 + | 商店权重 | ✅复用 |
| 同生共死 | Run | 桃园一人死,其余暴怒 +攻 | `mortal`→信号→全局 buff(REQ-022/event-when) | ✅复用 |
| 义结金兰 | Run | 蜀连携阈值 -1(更易凑) | `group-count` 阈值 | ✅复用 |
| 星球·桃园 | 星球 | 桃园 buff 档位 + | REQ-012 | ✅复用 |

**克制**:克够肥的中期;被速攻(没成型先崩)克。

### 3 ·「赤壁火攻」(吴·灼烧 Burn/AoE)
身份:点燃叠层 + 范围扩散,克密集守军。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **东南风** ⭐ | Build | 点燃叠层 | hitbox `dotPerTick/dotDuration` + `setMask:ST_BURNING`(done) | ✅复用 |
| 连营 | Run | 燃烧蔓延相邻 | hitbox AoE fan-out(done) | ✅复用 |
| 苦肉计 | Build | 自损血 → 全队法强 | 资源转换 + 全局 buff | ✅复用 |
| 火油 | Run | 燃烧伤害 + | dot 值 / 全局系数 | ✅复用 |
| 星球·火 | 星球 | 灼烧档位 + | REQ-012/013 | ✅复用 |

**克制**:克足轻人海(一锅端);被护盾/分散克。

### 4 ·「卧龙八阵」(谋士·控制 Control)
身份:群冻 + 法强滚雪球,苟到终盘爆发。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **八阵图** ⭐ | Build | 群冻 + 法强 | hitbox `setMask:ST_FROZEN`+`statusDuration`(done)+ steering `haltStatusMask`(done) | ✅复用 |
| 锁定 | Run | 冻结时长 + | `statusDuration` 值 | ✅复用 |
| 空城计 | Build | 残血时全队减伤 | `condition` 读 player_hp → 全局减伤资源 | ✅复用 |
| 借东风 | Run | 法强按回合涨 | banded by round + 全局 buff | ✅复用 |
| 星球·谋 | 星球 | 谋士羁绊档位 + | `group-count` | ✅复用 |

**克制**:克慢节奏对耗;**被刺客绕后(白衣渡江,撞 REQ-F-062)秒后排谋士**克。

### 5 ·「丈八蛇矛」(单核 High-roll)
身份:全资源喂张飞,斩杀通杀(赌脸)。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **蛇矛** ⭐ | Build | 全喂张飞·斩杀叠攻 | 单核=张飞强化模板变体(✅) + **斩杀(REQ-F-061)** | 🔴部分撞缺口 |
| 觅将 | Build | 张飞商店保底 | 权重/保底 | ✅复用 |
| 万军取首 | Run | 张飞击杀回蓝(连大招) | `self-rule` 自身 mana(REQ-021) | ✅复用 |
| 据点 | Run | 给张飞护盾 | per-unit → 模板/self-scope | 🟡重组 |
| 星球·勇 | 星球 | 斩杀档位 +(依赖 REQ-F-061) | REQ-012 | 🔴依赖 |

**克制**:克厚血单体;被群体铺场(单核淹没)+ 控制(冻住单核)克。

### 6 ·「乱世枭雄」(群雄·Flex)
身份:群雄"越少越强",灵活高 roll。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **枭雄** ⭐ | Build | 群雄越少越强 | `group-count` 数群雄 + REQ-013 `valueFrom`(反比) → 全局 buff | ✅复用 |
| 见风使舵 | Run | 洗一次全羁绊(转型) | 数据重置 | ✅复用 |
| 渔利 | Build | 非领先方经济 + | 读贡献名次 → 全局 econ | ✅复用 |
| 吕布 | Build | 群雄核心吕布强化 | per-unit → 模板 | 🟡重组 |
| 星球·群 | 星球 | 群雄档位 + | REQ-012 | ✅复用 |

**克制**:克版本固化;被纯雪球(自己不够肥)克。

### 8 ·「足轻人海」(Go-wide Tokens)
身份:铺满低费杂兵吃数量。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **人海** ⭐ | Build | 单位数越多·全体 + | `group-count` 数总单位 + `valueFrom` → 全局 buff | ✅复用 |
| 征召 | Run | 生成低费杂兵 | `self-rule` `spawn`(REQ-021) | ✅复用 |
| 蚁附 | Run | 杂兵死掉碎片喂经济 | `mortal.dropTemplate` + econ | ✅复用 |
| 屯垦 | Build | 人口上限 + | 阵容上限数据 | ✅复用 |
| 星球·众 | 星球 | 数量羁绊档位 + | `group-count` | ✅复用 |

**克制**:克单核(分摊伤害);**被赤壁火攻(密集被 AoE 一锅端)克**。

### 9 ·「屯田积粟」(Econ·Greed)
身份:攒利息滚经济,后期接管收割。

| 卡名 | 态 | 效果 | 数据形 | 判定 |
|---|---|---|---|---|
| **屯田** ⭐ | Build | 利息上限翻倍 | banded 档位翻倍 + REQ-013 | ✅复用 |
| 囤积 | Build | 不买人攒钱有额外利息 | `condition` 读 gold + banded | ✅复用 |
| 厚积薄发 | Build | 回合 N 后,攒的钱转战力 | banded by round + 一次性全局 buff | ✅复用 |
| 重农 | Run | 连胜/连败金 + | banded | ✅复用 |
| 星球·财 | 星球 | 利息档位 + | REQ-013 | ✅复用 |

**克制**:克长局对耗;**被速攻(前期被打崩,呼应虎豹铁骑)克**。

---

## 克制网(matchup 多样性的骨架,石头剪刀布闭环)

```
   速攻(虎豹铁骑) ─克─▶ 经济(屯田积粟) ─克─▶ 后期控制/单核 ─克─▶ ...
        ▲                                              │
        └──────────────── 被 控制(卧龙) 克 ◀───────────┘
   火攻(赤壁) ─克─▶ 人海(足轻)        刺客(白衣) ─克─▶ 后排谋士(卧龙)
   人海(足轻) ─克─▶ 单核(蛇矛)        单核(蛇矛) ─克─▶ 厚血单体
```

> **没有唯一最优解**:每个流派都有天敌也有猎物 → 三人攻岛时"看守岛太阁阵容 + 看邻居流派"临场调整,对决多样性从克制网长出来,不靠数值硬堆。10 套各有 ✅可落地路径,差异化爽点(斩杀/降将)清楚标了引擎成本。

> 复诵:「以战养战」≈ 纯数据,验证了卡牌系统的下限;「白衣渡江」压在缺口上,暴露了"抢人头"差异化的真实引擎成本。**D0 已闭环:2 真缺口入池(F-061/062),其余回驳/重组。下一步可进 D1 单机原型(首推「虎豹铁骑」,全 ✅、零缺口依赖)。**
