// 验收剧本 ②《六人德州》翻前顺时针轮转：UTG(座3)→座4→座5→主角(座0) 依次思考行动。
// 每 tick 驱动一个待行动 AI（=宿主 timer stepAI 同款真实路径）。座3/4/5 各行动一次（无论跟/加/弃都占一拍），
// 第 3 拍后轮到主角(座0·按钮·翻前第 4 个行动)。钉「轮到谁思考」的可观察节奏——非瞬时跑完。
// 与 AI 具体决策无关（跟/加/弃都推进一位）→ 结构性确定。
{
  "name": "翻前顺时针轮转到主角（第4行动·可观察轮次）",
  "game": "game-c",
  "seed": 20260718,
  "steps": [
    { "expect": [
      { "res": "actor", "eq": 3 },          // 起手：UTG 座3 先思考
      { "flag": "hero-turn", "eq": false }
    ]},
    { "tick": 3 },                            // 座3、座4、座5 依次各行动一拍
    { "expect": [
      { "res": "actor", "eq": 0 },           // 轮到主角(座0)
      { "flag": "hero-turn", "eq": true },   // 「轮到你思考」灯亮
      { "flag": "pending-ai", "eq": false }, // AI 停手·等主角
      { "sv": "street", "eq": "preflop" }
    ]}
  ]
}
