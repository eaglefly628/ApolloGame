# Game C ·《六人德州》游戏设计文档（GDD v1）

> **作者**：GD-C · 2026-07-17
> **状态**：🟢 v1——owner brief 固化（`README.md`）+ **核心八条已由 owner 问答拍板（2026-07-17·见 §11.5）**；数值细调与立绘/分级留开放（§11）。
> **承载**：owner 2026-07-17 拍板 C 位重启承载本设计；与已删旧作无关。
> **工作标题**：《六人德州》（备选：《德州之夜》《Hold'em Night》——待 owner 定名）
> **铁律对齐**：规则=标准德州，一致性无发挥空间；发挥空间全在 **AI 读牌难度、3D 牌房表现、角色卡代入**三处。

---

## 0. 一句话定位

> **单人 vs 五名行为树 AI 的六人桌德州扑克**：俯视固定 3D 牌房里，筹码真的会"哗啦"落在桌面上；
> AI 的强弱不靠作弊发牌，而靠"读你牌的准不准"——难度越高，它对你手牌范围的估计误差越小；简单档它根本不许读。

参照物：Prominence Poker / Poker Night 的"有房间感的单机德州"，减去剧情，加上可调"读牌"难度核。

## 1. 设计支柱

1. **规则零发挥**：打法与标准德州完全一致（owner 拍板）。正确性是底线——下注圈、边池、比牌 kicker 一处都不能错，全部测试钉死。
2. **可触摸的牌房**：俯视 3D 房间（牌桌+六凳）+ 筹码物理落桌。质感来自"筹码是真东西"，不来自视角炫技（相机固定不切）。
3. **诚实的难度**：AI 不偷看发牌堆、不改概率；难度=信息优势（对敌方手牌强度区间的读取误差 %）。可调、可关（简单档纯策略 AI）。
4. **你就是主角**：姓名+头像由外部角色卡带入（立绘字段预留），你的座位铭牌/结算都以你的身份呈现。

## 2. 核心循环

```
【一局（现金局·owner 拍板）】
  落座（角色卡带入身份·六人各带预设筹码+衣柜）→ 恒定盲注手手对局
  → 筹码尽则典当衣物续命 → 衣物也尽且缴不起盲注=剥光出局
  → 终局：主角出局（败）或五位姨太全部出局（胜）
      └─【一手牌 HAND 循环】（心跳单元）
           发底牌 → 翻前下注圈 → 翻牌(3) → 下注圈 → 转牌 → 下注圈 → 河牌 → 下注圈
           → 摊牌比大小（7 选 5 最优）/ 全弃直接收池 → 筹码物理推给赢家 → 按钮轮转 → ↺
```

节奏旋钮（现金局无盲注爬升压力，节奏靠这三个数据调）：盲注/起始筹码比 · 衣物总面值 · AI 典当阈值。

## 3. 规则面（全部摆成数据，逐条对照标准德州）

| 规则块 | 内容（数据表） | 备注 |
|---|---|---|
| 桌面结构 | 6 座位、按钮位、小盲/大盲轮转表 | 缺人（淘汰）时盲注轮转遵标准死按钮规则 |
| 盲注结构 | **现金局恒定盲注**（owner 拍板·默认 25/50，数据可调）；不设 SNG 爬升 | ✅ 已拍板（§11.5-1） |
| 下注动作 | fold / check / call / bet / raise / all-in；min-raise=上一加注增量；不足 min-raise 的 all-in 不重开行动 | 标准规则，无发挥 |
| 边池 | 多人 all-in 逐层切池；每池独立比牌 | 正确性关键，测试点名 |
| 摊牌 | 7 选 5 最优牌型；同型比 kicker；平分池，奇数筹码给位置靠前者；弃牌可 muck | 正确性关键 |
| 发牌 | 种子 PRNG 洗牌；烧牌可作数据开关（不影响公平，仪式感用） | 确定性、可回放 |
| 起始筹码 | 默认 1000（=20BB@25/50·浅码促行动），数据可调 | 数值细调开放（§11-b） |

> 归属：下注圈轮转/边池切分是状态机算法，Condition→Event→Effect 表达不了——依 **owner 2026-07-17「本项目允许 TS」口径**
> 落为 game-c 内确定性 TS 模块（`capability-plan.md` §4 逐条记账·测试钉死），不等引擎池腾槽。

## 3.5 衣物筹码——典当机制（owner 2026-07-17 追加）

> 每位牌手（主角 + 五位姨太对手）随身一套**衣物清单**（如：上衣/裙子/袜子/手套/耳环/内衣……每件一个筹码价值）。
> 筹码告急时，点自己身上的一件衣物 → **原子换成筹码**入自己筹码栈。衣物点完+筹码归零=真正出局。

- **经济语义**：衣物=带面值的可典当资产。典当走现成 `craft-recipe`（信号→原子扣衣物+加筹码），**每件衣物一条配方=纯数据**，零新代码。
- **胜负弧线（owner 拍板）**：淘汰条件=「筹码=0 且无衣可当（下一手盲注缴不起）」→ **剥光才出局**；终局=主角败或五姨太全出局。
- **信息面（owner 拍板）**：点开任一座位卡看**具体件名+面值**（"她只剩内衣了"的压迫感 + 可精算对手续命能力）。
- **AI 典当策略**：行为树动作叶——筹码低于压力线（默认 <3BB）时典当最低值衣物；性格影响典当阈值（岩石型早当保守打，诈唬狂拖到 all-in 才当）。
- **衣物表（数据·面值待调平衡）**：耳环 100 · 手套 150 · 袜子 200 · 上衣 500 · 裙子 500 · 内衣 1000（总 2450）；**典当顺序自由点选**（owner 拍板·点哪件当哪件）。
- **表现口径（owner 拍板 2026-07-17 第三批）**：**逐件图层立绘立项**——五位姨太各一套分层立绘（底层+每件衣物一图层，典当哪件消哪层，约 5×7=35 层）；主角立绘仍走角色卡预留字段（外部给则显示，无则头像）。**尺度基线：不露骨**——最终层（内衣也典当后）用剪影/遮挡处理；内容口径=自玩/内部体验（不做发行准备，将来要发再议分级）。立绘美术为独立工作项（台账建行+生成排期随美术线真 key 节奏）。

## 4. AI 设计（行为树 + 读牌误差）

- **决策骨架 = 行为树（数据）**：节点输入只有四类——①自身手牌强度（equity 估算）②位置（前/后位）③底池赔率 ④对手行动史（本手加注次数等）。输出=动作+加注尺度（尺度表=数据：1/2 池、2/3 池、全池、all-in）。
- **读牌 oracle（难度核）**：AI 对"对方当前手牌强度"拿到一个**区间估计**：`真实强度 ± 误差%`（误差由难度表配置，owner 可后调）。误差收敛=难度上升。**简单档 oracle 关闭**——AI 只按自身牌力+赔率打（正常策略 AI）。噪声用种子 PRNG，同局可复现。
- **性格模板（数据表）**：紧凶 / 松凶 / 岩石 / 跟注站 / 诈唬狂——每模板一组参数（进池率、激进度、诈唬频率、oracle 权重）。五名对手从模板池配置。
- **不作弊承诺**：AI 永不读发牌堆、永不知道未发的公共牌；oracle 只提供"对手成牌强度"的带噪估计。

## 5. 3D 牌房与筹码物理（表现层）

- **房间**：俯视角固定相机（不切换、不旋转）。布局=牌桌居中 + 六把凳子环绕 + 地板/墙（盒庭 3D 线现成组件拼）。
- **筹码**：3D 圆柱堆按面额分色。下注/收池时筹码**抛掷落桌，刚体物理表现**（弹跳、散落、堆叠）。
  **纪律**：物理只管好看——筹码**数量**永远是 sim 内确定性资源，物理落点不回写游戏状态（render-only，不碰确定性/回放）。
- **牌**：公共牌在桌面 3D 平铺翻开；自己的底牌另有 UI 大图（俯视角下看清花色）。
- 特效：发牌滑弧、翻牌翻转、底池推移、胜者聚光；声音：洗牌/发牌/筹码碰撞/翻牌/allin/胜利（全走现有音频线，声音=数据）。

## 6. 主角与角色卡

- 外部角色卡带入：**姓名 + 头像**（本期），**立绘字段预留**（后期站桌边/结算演出用）。
- 桌上呈现：主角座位铭牌（姓名+头像+筹码）；结算屏以主角身份收尾。
- 带入通道待探明：工坊/卡带 meta 是否已有玩家档案位——没有则提 REQ 给 PST/Lead（§11-e）。

## 7. 美术库（按美术管线台账制）

产 `art-ledger` 逐行台账（game-g/d 先例）：桌呢绒面、卡背、52 张牌面（或程序化+皮肤槽回退）、
筹码面额贴图 ×5、头像框、五位姨太头像、衣物件目图标 ×6、房间地板/墙、凳子皮。
**分层立绘包（owner 拍板逐件图层方案）**：五位姨太 × [底层+耳环/手套/袜子/上衣/裙子/内衣 各一层] ≈35 层，
台账逐层建行（spec 统一画布/锚点对位·**不露骨基线：底层=剪影/遮挡处理**）；衣柜面板左侧立绘区按已穿图层实时合成。
3D 主体走盒庭线"几何生成+贴图 key 皮肤槽"路线，皮肤未就绪时程序化回退（观感承诺同 game-q/t）。
主体视觉实体必有皮肤槽（capability-plan §4.5 红线）。

## 8. UI（现有 LayoutNode 闭集，零发明）

行动条（fold/check/call/raise+尺度滑杆）、六座位铭牌（姓名/头像/筹码/状态/倒计时+**衣物件数徽章**）、底池与当前注显示、
手牌与牌型提示、结算面板、盲注级别牌。
**衣物典当面板（§3.5 的显示答案·全部现有控件）**：点任一座位卡（Clickable/Button 信号）→ 弹 Panel 覆盖层=该角色衣柜：
每件一行（图标 Image + 名称 Label + 面值 Label + 状态）；**自己的**衣柜行尾带「换筹码」Button（发典当信号→craft-recipe 成交，
行置灰）；**对手的**只展示剩余状态不可点。关闭回桌。后期立绘到位可升级为立绘分区热点点击（美术另立项）。
**布局等 owner 侧 cross design 稿到位后接**；先按 game-i 范例做可用素坯。控件缺口一律 requests.md 报 PUI，不手写逃生。

## 9. 实现顺序（逻辑先行 · 八阶段流程板管进度）

- **M1 逻辑核**（headless·owner TS 口径）：`holdem-eval.ts`（7 选 5+kicker）+ `betting-engine.ts`（下注圈/边池）+ 发牌流，全规则测试钉死（含边池/分池/kicker 边角）。
- **M2 AI**：行为树+oracle+性格表，AI 对 AI 万手模拟验合理性（胜率分布、非退化策略）。
- **M3 3D 牌房**：房间+筹码物理+发牌演出（render-only）。
- **M4 UI 接线**：素坯→cross design 稿落位。
- **M5 打磨**：特效/声音/难度调参。
- 每阶段一个会话、过流程板门（`game-production.md`），不一口气跑穿。

## 10. 确定性声明

随机源=引擎种子 PRNG（洗牌、AI 噪声、oracle 噪声全部走它，游戏层禁裸 Math.random）；
单人游戏，无 lockstep 需求，但保持全 sim 确定性（回放/测试/bench 复现）。筹码物理与特效=render-only 旁路，不入 hash。

## 11.5 拍板记录（owner 问答 · 2026-07-17）

1. **局型=现金局**（恒定盲注 25/50 默认；不做 SNG 爬升）。
2. **读牌=仅 AI 用**（难度=误差%；玩家无对手范围提示）。
3. **淘汰=剥光才出局**（筹码 0 后可典当续命；衣尽+缴不起盲注=出局）。
4. **主角衣柜=游戏预设统一**（角色卡只带姓名+头像，REQ-C-104 通道保持最小）。
5. **典当顺序=自由点选**。
6. **对手衣柜=具体件名可见**（点座位卡看件名+面值）。
7. **阵容=大姨太~五姨太五位**（一对一配五性格模板：紧凶/松凶/岩石/跟注站/诈唬狂）。
8. **声音=合成音先行**（真采样后补即换）。

第三批（2026-07-17 续）：
9. **立绘=逐件图层方案**（五姨太×每件一层≈35 层·典当消层·不露骨基线·独立美术工作项）。
10. **内容口径=自玩/内部体验**（不做发行准备；将来要发再议分级）。
11. **性格对位=按长幼配**：大姨太=岩石·二姨太=紧凶·三姨太=诈唬狂·四姨太=跟注站·五姨太=松凶。
12. **数值基线=默认先行**：起始 1000（20BB@25/50）·衣物总值 2450·AI 典当线 <3BB——M2 万手 sim+试玩后调。

另（讨论中默认成立·未异议）：oracle=对手当前成牌强度百分位+种子噪声（**不做蒙特卡洛**），误差%随难度表可调（默认 normal ±30 / hard ±10 / easy 关）。
布局设计文档=Claude 出稿（owner 指令）：`ui-brief.md` + `layout-mockup.html` ✅——原 §11-g 撤销。

## 11. 仍开放（不阻塞 M1）

- **数值细调**：全部默认值 M2 万手 sim + 试玩后调（已拍板口径不再逐项问）。
- **立绘排期**：分层立绘 35 层的台账建行与生成走美术线（等真 key 节奏）；层规格（画布/锚点）建行时定。
- **姨太人设细节**：名字之外的头像描述/口头禅等，美术台账建行前由 GD 出人设卡备审。
