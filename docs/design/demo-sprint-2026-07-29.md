# 7·29-30 审核冲刺纲领（Demo Sprint · owner 2026-07-07 定向）

> **审核方要验什么**：这台引擎能不能**用自然语言描述，快速生成 playable 的游戏**。
> **量化口径（owner 转述）**：①一周能出 ~30 款；②以**换皮**（美术资源换皮）为主力量产手段；③基本玩法 OK 即可（先投放素材验证，再精加工）。
> **当前最大管线缺口 = 美术**：生成的游戏是程序化色块（game-q 实证：零真资产），审核观感立死。必须把美术 API（千问 2D wanx / Tripo 3D·Meshy 可插）**自动接进生成流**。
> 本纲领=Lead 图纸总纲；分单见 `docs/workflow/requests.md` REQ-DEMO-0729 系列。**冲刺期内本纲领优先级压过一切非冲刺工单。**

## 一、核心洞察：美术自动化不需要动引擎

数据接口早已存在——`art:<英文关键词>`（`src/assembly/resolve-art-refs.ts`）：LLM 只写查询串，选材发生在引擎解释器里（确定性排序、留痕可审计）。缺口只是：**素材库没有的东西，现在解析失败退化成占位**。

对齐方案 = 在**生成/保存时**加一步确定性编排（服务层，非引擎）：

```
自然语言 → LLM 生成 manifest（含 art: 引用 + 一个 artStyle 风格锚·纯数据）
        → 编排器逐条解析 art: 引用：
            库内高分命中 → 直接用（免费·秒回）
            无命中/要独占风格 → 调 ai-gen API（千问2D/Tripo3D）生成
                → 落游戏本地美术目录 + provenance 硬字段 + 登记本地索引
        → art: 引用钉死成具体资产 id（构建期解析·可审计）→ 可玩卡带
```

- **对弱 LLM 零新负担**：还是只写 `art:关键词`；新增的 `artStyle` 是一个纯数据块 `{stylePrompt, palette, mode: library|generate|auto}`——模板里带默认值，LLM 不填也能跑。
- **换皮=同一 manifest × 新风格锚**：玩法数据一字不改，重跑美术编排 → 新卡带。这就是 30 款/周的乘法器。
- **无 key 不阻塞**：mock 兜底（占位图+MOCK 标记+凭证探针输出），流水线永远走得通；有 key 自动升真。
- **成本控制**：内容寻址缓存——`hash(provider+prompt+参数)` 相同 → 复用已生成资产，换皮批量时同风格素材免重复扣费。

## 二、人审门两轨制（Lead 裁决 2026-07-07·报 owner 知情）

M2.5 人审门（AI 产物→pending→人批准才入库）与 30 款/周吞吐冲突。裁决拆两轨：

- **游戏本地轨（新·快速通道）**：生成流产出的资产**直接接线进该游戏**（provenance 硬字段照旧+标 `unreviewed`），游戏立即可玩——demo 与量产走这轨。
- **共享货架轨（不变）**：任何资产要进**共享库 index**，仍必须过 M2.5 人审门。精加工阶段从本地轨挑好料走人审入库。
- 原则没破：人审门守的是共享资产库不被污染；游戏本地资产=该游戏私产，出货前有评分卡+审计兜底。**owner 若否决此裁决，冲刺方案需重排。**

## 三、分轨与派工（T1-T5·按序）

| 轨 | 内容 | 域/指派 | 期限 |
|---|---|---|---|
| **T1 美术自动生成接线（P0·冲刺核心）** | 生成/保存流加美术编排步：art: 逐条 库查→不中则 ai-gen 真调（千问2D 先行）→落本地+provenance+登记→引用钉死；artStyle 风格锚数据块；模板/系统提示改「主体视觉实体默认 Sprite+art:」（可换皮的前提=有皮肤槽，杜绝 game-q 式全程序化色块）；进度条上必须有「生成美术 n/m」阶段灯 | PST（apollo.py/生成流）+ PA（契约：brief schema/本地索引登记/内容寻址缓存/adapter 口径·Meshy 插槽） | 7/13 |
| **T2 换皮流水线** | studio 一键 reskin：选已有卡带→给新风格锚→只重跑美术编排→存成新卡带（slug-v2）；批量模式（一套玩法 × N 风格锚） | PST | 7/18 |
| **T3 量产吞吐验证** | 批量冒烟脚本（batch-gen-smoke·新建）：mock LLM+mock 美术连出 N=10 款 e2e（生成→美术→parseManifest 零 error→audit 无新红旗）·退出码门禁；真 key 抽 3 款全真跑通并记录耗时/成本/token | 指派：Opus 档施工·Lead 出 spec | 7/20 |
| **T4 3D 线（可选·不进关键路径）** | 3D 游戏的 Model3D 自动接线（Tripo 文生 3D 已有真调路径 `handle_asset_generate`）；demo 若含 3D 展示位再拉 | P3D 知会·待拉动 | 7/24 |
| **T5 彩排** | 全真 demo 演练 ×2：现场口径=「一句话描述→几分钟→可玩+有皮」+「同款换 3 套皮」；评分卡抽检；掉链子的尾巴清完 | Lead 主持·owner 验 | 7/27 |

## 四、冲刺期队列重排（压其他工单）

- PST 队列改为：**T1 美术接线（即刻·P0）** → 心跳/阶段灯余项（并入 T1 的进度可视化）→ T2 换皮 → ~~音频 B 件~~（**冲刺后**）→ M1 货架墙（冲刺后）。
- REQ-3D-像素断言、REQ-CAP-改掷收编、game-g v3 落地等一律冲刺后（game-g v3 设计讨论可继续，不占施工位）。
- 引擎域冻结非必要改动——demo 稳定性优先；真撞引擎缺口走 requests 由 Lead 裁。

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| API 配额/成本（30 款×每款 10-30 资产） | 内容寻址缓存复用；库命中优先；千问 wanx 单价低者先行；T3 实测记录单款成本给 owner |
| 生成时延（文生图秒级·文生3D 分钟级） | 2D 先行为主力；美术编排并行发请求；进度阶段灯保体感 |
| 风格散（30 款像零件堆） | 风格锚强制注入每条 prompt；换皮批共享锚 |
| 生成质量翻车 | 本地轨不挡可玩性；评分卡抽检+精加工阶段人工换料（M2.5 轨） |
| deepseek/低模生成不稳 | 已有低模四件（模板起步+词汇裁剪+错误指令化）；demo 用稳定 provider，低模路线另测 |

## 六、demo 判定口径（彩排即照此验收）

1. 现场输入一句自然语言 → 出可玩卡带（含真美术皮）——端到端耗时记录在案。
2. 同一款玩法现场换 2-3 套皮，各成独立卡带。
3. 出示量产证据：T3 批量冒烟报告（N 款/耗时/成本）折算周产能 ≥30。
4. 出示治理证据：provenance/审计/评分卡——「快而不失控」是我们对比裸 LLM 写代码路线的护城河。
