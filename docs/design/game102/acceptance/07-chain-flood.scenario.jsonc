{
  "name": "连锁炮·一发清连通同色区",
  "seed": 20107,
  "gdd": "special-cannons.md §2",
  "intent": "对抗目标：去掉 flood 邻接传播（只清命中 1 格）→ remain.blue 停在 4（应 0）",
  "level": {
    "cols": 6, "rows": 1,
    "palette": ["blue", "red"],
    "ammo": 20, "conveyorCap": 5, "burstCap": 10, "slots": 5, "beltSpeed": 95,
    "limit": { "kind": "moves", "n": 3 },
    "goals": [ { "kind": "clear" } ],
    "bitmap": [ "000001" ],          // 连通 blue×5 + red×1（连锁只走同色连通区·不碰 red）
    "grant": { "chain": 1 },         // 强制激活 1 发连锁炮
    "specialAmmo": { "chain": 1 }    // 本剧本连锁定为一发（凸显 flood：一发清整片）
  },
  "steps": [
    { "do": ["tapSupply:chain"], "then": "tick:60",
      "expect": { "remain.blue": 0, "remain.red": 1, "remain.total": 1, "flow": "playing" } }
  ]
}
