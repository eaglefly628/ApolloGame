{
  // PENDING · 依赖 REQ-G102-SPECIAL ② 连锁炮(flood·或下沉 flood-clear) + adapter grant/tapSupply:chain。落地后改名激活。
  // 对抗目标：去 flood 邻接传播（只清命中 1 格）→ remain-blue 停在 4（应 0）。
  "name": "连锁炮·一发清连通同色区",
  "game": "game102",
  "seed": 20107,
  "config": { "cols": 6, "rows": 1, "palette": ["blue", "red"], "ammo": 20,
    "conveyorCap": 5, "burstCap": 10, "slots": 5, "beltSpeed": 95,
    "limit": { "kind": "moves", "n": 3 }, "goals": [{ "kind": "clear" }], "bitmap": ["000001"],
    "grant": { "chain": 1 } },
  "steps": [
    { "tick": 2 }, { "signal": "tapSupply:chain" }, { "tick": 60 },
    { "expect": [ { "res": "remain.blue", "eq": 0 }, { "res": "remain.red", "eq": 1 }, { "sv": "flow", "eq": "playing" } ] }
  ]
}
