# game102《色流工坊 / Pixel Pour》· 核心玩法策划案（单一真相 · 权威）

> **本文件 = game102 核心玩法的唯一权威真相**（owner 2026-07-25 定稿·PE/GD 录）。实现、验收、UI、关卡设计一律以此为准。
> 参考实机图（唯一复刻基准·看图优先）：`ref/pixelflow-ref-01~03.png` + `ref/IMG_6061/6063/6064`。
> 历史演进记 `core-experience-v2.md`（§2.1 owner 口述细化 / §2.2 权威总述 / §3 能力复盘）——本文件是其整理定稿。

---

## 0. 一句话定义

**带容量限制的动态颜色匹配 + 队列调度谜题**：把"发射器"按正确的**颜色 / 顺序 / 时机**投上环绕像素画的传送带，让它们绕行时消掉暴露的同色像素，在有限的传送带容量 + 5 个等待槽内做流量调度，清空整幅画即胜、资源死锁即负。**核心是动态流量管理（Traffic Management），不是手速、也不是静态消除。**

## 1. 底层操作循环

```
点击部署 → 传送带流动 → 自动索敌消除同色 → 缓冲区回收 → （再调度）
```

## 2. 元素定义

| 元素 | 说明 |
|---|---|
| **中央像素画** | 由多种颜色方块（体素格）构成的图案。每格有颜色 + 1 点 hp。清空全部 = 胜利。 |
| **发射器（射击器/炮台/打蛋器）** | 带**一种颜色** + 头顶**弹药数字**。部署后进入传送带绕行，**自动**击破**与自身同色**的暴露像素格。图标 = 圆角方体 + 顶部两根竖柱 + 面上动态弹数（实机为带色动物/打蛋器模型）。 |
| **传送带轨道** | 环绕像素画一周的跑道（圆角矩形·有向·持续移动）。发射器从**弹簧口（左下~7点）出发**，绕行一圈。**有严格容量上限**（见 §4）。 |
| **等待槽（缓冲区）×5** | 屏幕底部 5 个缓存槽，存放"绕完一圈但弹药未耗尽"的返回发射器。点击槽中发射器 → 重新推入传送带。 |
| **待发弹库（池）= ⭐递进队列** | 等待槽下方的**有限、有序、递进的**发射器队列——**内容（每个 {颜色, 弹药}）+ 顺序，由本关像素图的颜色/数量决定**（守恒：Σ弹药==Σ格·见 §6），**有意编排、非随机、非无限取**。取走队首一个部署 → 后面**依次前推一格补位**（递进）。**玩家对"部署哪个、什么时机"的选择 = 本作挑战性与乐趣的核心来源**（错序 → 拥堵/死锁）。⚠ 现代码的"每色无限分发器"是占位实现·**将被本递进队列替换**。 |

## 3. 消除 + 弹药系统

- **颜色匹配**：发射器**只能消除与自身同色**的像素格（异色不误伤）。
- **过位剥离（次序依赖）+ 暴露判定（精确规则·owner 参考稿）**：只有**暴露**的同色格可被锁定消除。**暴露 = 该格至少有一个正交相邻格为空**（被同伴四面包住 = 未暴露 = 打不到）。每消掉一格 → **只需更新其相邻格的暴露状态**（进/出"可锁定列表"）。故像素层叠/包裹排列时必须**从外向里**层层剥：先清外层 → 内层才暴露 → 才可被对应色打到。
  - *引擎实现*：确定性**网格相邻检索**（非物理射线——本引擎确定性 ECS 无 raycast）。现代码用"有限视野 sightRadius"近似"外沿暴露"，精确的邻空暴露判定待作为目标过滤接入（暴露格打 EXPOSED 标位·消格时更新邻格）。
- **弹药 = 可消同色数上限**（头顶数字）。**每消除一格 −1**。
- **弹药归零 → 发射器物理退场**（消失·不占等待槽）。
- **选错色 / 该边无暴露同色**：绕一圈打不到任何同色 → 弹药不减、满弹返回等待槽。

## 4. 空间与调度模型（核心难点）

- **传送带容量上限**：轨道 = 环形路径均分为 **N 个不重叠插槽**（`Max_Capacity` 示例 ≈ 6）；发射器在插槽间匀速移动，**前方插槽被占 → 当前必须停下（不穿模）= 拥堵**。**投放数量 / 顺序错误 → 拥堵**。
- **射击锁定**：发射器锁定一个目标格开火时**独占该格引用**，防止其他同色发射器**重复锁同一格**（一格只被消一次）。
- **等待槽回收**：发射器绕完一圈**仍有弹药**（如同色被别色包围、这一圈没打到）→ **退出传送带、落入一个空等待槽**；玩家**择机再点它 → 重新送回传送带**。
- **优先级调度**：玩家在**有限缓冲（5 槽）+ 有限管道（传送带容量）**内，对不同颜色的处理进程排优先级。
- **资源验算**：实时权衡"可用发射器剩余弹药总量 vs 目标同色格数"。**过量 / 错序投放 → 无效发射器占满传送带 + 等待槽 → 拥堵死锁**。

## 5. 状态判定

- **胜利（Win）**：中央所有像素格被清空。
- **失败（Fail = 资源死锁 / Deadlock）**：**传送带达到容量上限 且 5 个等待槽全部填满**。此刻既无正在输出的发射器、也无法部署新的 → 判负。

## 6. ⭐ 核心经济：弹药守恒

- **全部发射器的弹药之和 == 像素画全部体素格数**（出的子弹总数 == 要消的格子总数·**精确相等·零冗余**）。
- ⇒ 必须让**每一发都打在暴露的同色格上**（不空过、不带弹堵槽），且**按外→内暴露顺序 + 对应色**投放，才能清完全盘且不触发死锁。**这是谜题内核**：确定性资源调度（守恒进 hash·可复算·天然契合确定性引擎）。

## 7. 操作（输入）

- **部署**：点击待发弹库 / 等待槽中的发射器 → 送上传送带（从弹簧口入场）。取走后队列前推补位。
- **回收再部署**：点击等待槽中的返回发射器 → 重新推入传送带。
- （无拖拽、无手动瞄准——发射器**自动**索敌开火。）

## 8. 关卡设计约束（authoring）

1. **Σ(发射器弹药) == Σ(像素格数)**（守恒·出题即校验）。
2. 像素图分**层叠/包裹**结构 → 隐含**外→内**的正确清除序。
3. **待发弹库的内容 + 顺序**是关卡的核心难度旋钮（有意编排的出场序）。
4. 每关分段推进 `x/5`（实机左下角）。
5. 画内可嵌 🔑钥匙 / 🔒锁块等目标/解锁件（关型变体）。

## 9. 引擎实现映射（现状 → 缺口·详见 core-experience-v2 §3）

| 机制 | 能力 | 现状 |
|---|---|---|
| 像素板 / 按色计数 | BoardCell 实体阵 + `group-count` | ✅ 已落 |
| 炮绕带一圈（弹簧出发） | `PathFollow{loop:false}` | ✅ 已落 |
| 过位剥离（外→内） | `Perception{有限视野}`+`aggro`+`hitbox` | ✅ 已落 |
| 胜利 | `flow`（remain.total≤0） | ✅ 已落 |
| 炮台图标+弹数 | box+竖柱+`Text`/`text-binding`（矢量）或 `Sprite`（真贴图） | 🔶 矢量已落·真贴图待 owner 定 |
| **弹药 per-shot 扣 + 归零退场** | 需「命中即扣发射源一发」 | ⚠ **REQ-SPENDONFIRE**（下沉主程） |
| **传送带容量 + 拥堵 + 空槽分配 + 死锁真相** | 需「有限容量有序队列/传送带」 | ⚠ **REQ-CONVEYOR-CAP**（下沉主程·核心难点） |
| 死锁 flow 条件 / 队列前推 / 守恒 | `flow` 条件树 + `tray`/`zone-occupancy` + 关卡数据 | 🔷 待上两条落地后纯数据组合 |

**build 序**：主程落 REQ-SPENDONFIRE + REQ-CONVEYOR-CAP → PE 接 per-shot 弹药 + 容量/空槽/死锁数据 → 关卡 authoring（守恒+出场序）→ 炮台真贴图 + 队列/池 UI（PUI）→ 3D 盒庭（P3D）。

---

## 附录 B · owner 参考稿（工程实现细节）= 仅参考·不照搬架构

owner 另提供一份工程稿（Unreal/Unity/Cocos 口径：`Voxel Manager 空间哈希` / `Spline 样条 + N 虚拟插槽` / `Dictionary<ColorID,List<Voxel>> ExposedVoxels` / `发射器 FSM: Spawn·Moving·Shooting·Buffer·Dead`）。**其玩法规则精化已吸收进正文（暴露判定 §3 / 插槽不重叠+容量 §4 / 射击锁定 §4）**；但**那套 FSM/Spline/Dictionary 架构不照搬**——本作走 **ZeroCraft 确定性 ECS：能力（PathFollow/aggro/hitbox/group-count/flow…）+ 纯数据**，暴露判定用**确定性网格相邻检索**（非物理 raycast）。owner 明示"工程稿只是参考·玩法核心最重要"。

## 附录 A · ⚠ 研究稿中的「连线解谜」= 另一款游戏（**不属于 game102·不实现**）

owner 提供的研究稿末段描述了一套**《Flow Free》式「拖拽连接同色端点、填满网格」的连线解谜**（`M×N 网格 / CellType Endpoint·Path / Connections 4-bit 位掩码 / OnPointerDown-Drag-Up 拖拽 / SequenceIndex 回溯·截断 / 端点连通判定 / 16 图集管道渲染`）。

**评判（先评判·PE 2026-07-25）**：该机制与本作「炮台+传送带+等待槽的实时流量调度」**完全不同、不可混合**（一个实时调度、一个回合制拖线）。原文虽自称"Pixel Flow"，实为 **Pixel Flow（本作）↔ Flow Free（连线解谜）名字混淆**。**故本文件正文（§0–§9）不含连线解谜任何要素**；此段仅作研究留档。若日后要做连线解谜，另立新游戏（非 game102）。
