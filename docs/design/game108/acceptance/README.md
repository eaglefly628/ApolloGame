# game108 验收剧本 · 动作词表与机读态

**剧本作者 = GD**（条款出处 `gdd.md §15` 表·S2 时已写定）；**PE 修码不改剧本**——
剧本错 = GD 改并留痕（owner 2026-07-17「绿门不可玩」复盘立的循环律）。

## 动作名（`signal`）——就是屏上的 `data-action`【R-108-70·词表对齐律】

| 动作 | 含义 |
|---|---|
| `charge.rock` / `charge.paper` / `charge.scissors` | T1 蓄该手（+1·封顶 3） |
| `throw.rock` / `throw.paper` / `throw.scissors` | T2 出该手 |
| `smoke.use` | 烟雾（已接线） |
| `duel.next` | **T4 玩家闸门**（【R-108-05】v3·结算屏点它才进下一回合）／终局屏点它 = 宿主重开一局。**已接线** |
| `shard.pick` | 选碎片（**尚未接线**·凿改层未开工·S4 后续） |

唯一真相 = `games/game108/theme.ts` 的 `ACT`。屏上按钮的 `action`、DOM 的 `data-action`、
本目录剧本的 `signal`，**三处同一串字符**——机器才点得动（game-a 曾因两套词表各写各的，可驱动率 0/19）。

`by:"p2"` = **剧本指定对手这回合出什么**，等价于人类对手按了那个键；
**不是 AI 决策**（AI 自己选哪只手 = 【R-108-30】·见 `requests.md REQ-108-ENG-05`）。

## 机读态（断言目标）

| 名字 | 是什么 |
|---|---|
| `res: p1.hp` / `p2.hp` | 双方血量（**投影**：两侧 Resource 同 id `hp`，全局断言分不清侧，故适配层投成各侧唯一名） |
| `res: <侧>.charge.<手>` | 六条蓄力槽（本就各侧唯一·直读） |
| `sv: flow` | 当前时区（**投影**：`GameFlow.current` 不是标量组件） |
| `sv: <侧>.lastThrow` | 该侧本回合出的手 |
| `flag: <侧>.dead` | 该侧是否已倒下 |
| `res: p1.budget` | 【R-108-10】v3 本回合还剩多少蓄力额度（1=还能蓄一层·0=这回合蓄过了） |
| `res: p1.debt` | 【R-108-04】v3 本回合已欠的罚血点数（T1 开场清零） |
| `flag: duel.next.armed` | 【R-108-05】v3 玩家闸门旗（按下「下一轮」置位·T1 开场复位） |
| `sv: flow` 的两个新值 | `throwPenalty` / `throwPenaltyHit` = **T2 的读秒尾巴，不是第五拍**（屏上仍写「出招/拖延中」） |

## 跑法

```
npx vite-node scripts/acceptance-run.mjs -- --game game108
```

## 当前状态（2026-08-08 · v3 节奏案落地后）

**12 绿 / 0 红**（`npx vite-node scripts/acceptance-run.mjs -- game108`）。

v3 重写的账：
- ①②③⑤⑦ **全部改成按回合计**。v2 写的是「同一个 T1 里 `charge.rock` ×2 → −30」——
  描述的是"按几次键"，而它在**「一回合一层」和「每点一次一层」两种实现下都成立**，
  于是【R-108-10】的漂移躲过了三个阶段没人发现（gdd §15 写法铁律的第一个受害者）。
- ⑧ `08-timeout-carry` **整条删除并替换**为 `08-timeout-penalty`：v2 的「超时顺延」被
  【R-108-02】v3 作废（v2「不罚玩家不卡节奏」→ v3「罚，而且卡到你出手」）。
- 新增 ⑪ `11-t1-timeout-no-charge`（【R-108-02】v3）· ⑫ `12-t4-player-gate`（【R-108-05】）。

`gdd.md §15` 还有 4 条（改表 / 增维 / 逆转 / 确定性）尚未转成文件——前三条依赖凿改层（未开工）。

**「罚血真扣血」已接上**（2026-08-08）：⑧ 现在同时钉台账与血量（`p1.debt` 4 → `p1.hp` 96、
对手 `p2.hp` 一点不掉）。**两个都要断言**——只钉台账的话，「屏上写着欠 4 点、血条纹丝不动」
这种最难查的假象照样全绿（接线第一版漏建节拍旗实体，正是被这条血量断言逮住的）。
