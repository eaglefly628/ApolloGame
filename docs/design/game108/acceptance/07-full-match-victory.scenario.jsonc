// GD-108 验收剧本 ⑦ · **一整局打到终局**（S4 核心循环：开局 → 行动 → 反馈 → 终局）**v4 重写**。
//   玩家每回合蓄一层、出**克得掉复读机下一手**的那只 → 每回合打 10+1×10 = 20，五回合 100 ⇒ 终局。
//   **每回合都得点「下一轮」**（【R-108-05】玩家闸门）。
//   这条是「这游戏到底能不能玩完」的机器证——不是单点规则，是**整条循环**。
//
// ⚠ v2 这条写的是「三回合满蓄布打死」——那正是在断言 R-108-10 的 bug（一个 T1 连点三次直接满蓄）。
//   v3 一回合一层，同样的打法要五回合；这个"变慢"本身就是条款生效的证据。
//
// ⚠ v4（2026-08-15·【R-108-35】）：v3 写的是「五回合**都出布**打死复读机」，
//   而那要求复读机**恒出石**——那不是条款，那是 bug 的形状（owner 试玩：「AI 永远只会出锤子」）。
//   条款（gdd §9.4）是「倾向复用上一回合**赢过的**手」：它开局出石，我用布赢它，
//   **下一回合它就改出布**。所以正确的打法是跟着它转：布 → 剪 → 石 → 布 → 剪。
//   这一串本身就是第一档要教的东西——「对手是有规律的，读出来你就能连赢」。
//   **v3 那本剧本是这个 bug 活了半个月的原因之一：它把 bug 写成了验收基准。**
// 对抗目标（改坏应变红）：
//   · 复读机退回"恒出石/复用自己上一手" → 第 2 回合我出剪它出石 ⇒ 我挨打，血量断言红。
//   · 胜负判定撤掉（self-rule → dead flag 那条按侧通路）→ flow 永远停在 settle → 断言红。
//   · AI 不出招 → 永不结算 → 血不掉 → 断言红。
//   · T4 闸门撤成自动推进 → 中间那条「不点就停在 settle」断言红。
{
  "name": "整局闭环·五回合读着复读机的规律连赢打死它",
  "game": "game108", "seed": 108,
  "steps": [
    { "expect": [ { "res": "p2.hp", "eq": 100 }, { "sv": "flow", "eq": "charge" } ] },

    // ── 第 1 回合：它开局出石（唯一的确定态）→ 我出布 ──────────────────────
    { "signal": "charge.paper" }, { "tick": 2 },
    { "expect": [ { "res": "p1.charge.paper", "eq": 1 } ] },
    { "tick": 275 },
    { "expect": [ { "sv": "flow", "eq": "throw" } ] },
    { "signal": "throw.paper" }, { "tick": 3 },
    { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "sv": "flow", "eq": "settle" },
      { "res": "p2.hp", "eq": 80 },             // 【R-108-13】10+1×10=20
      { "res": "p1.hp", "eq": 100 },
      { "res": "p1.charge.paper", "eq": 0 },    // 【R-108-14】出过即清零
      { "sv": "p2.lastThrow", "eq": "rock" },   // 开局那只石
    ] },
    // 【R-108-05】不点就不走：干等 10 秒仍停在结算
    { "tick": 600 },
    { "expect": [ { "sv": "flow", "eq": "settle" } ] },
    { "signal": "duel.next" }, { "tick": 3 },
    { "expect": [ { "sv": "flow", "eq": "charge" } ] },

    // ── 第 2 回合：布赢了 ⇒ 它复用布 → 我出剪 ────────────────────────────
    { "signal": "charge.scissors" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.scissors" }, { "tick": 3 }, { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "res": "p2.hp", "eq": 60 },
      { "res": "p1.hp", "eq": 100 },              // 读对了就一滴血不掉
      { "sv": "p2.lastThrow", "eq": "paper" },    // 【R-108-35】它复用的是上一回合赢过的那只
    ] },
    { "signal": "duel.next" }, { "tick": 3 },

    // ── 第 3 回合：剪赢了 ⇒ 它复用剪 → 我出石 ────────────────────────────
    { "signal": "charge.rock" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.rock" }, { "tick": 3 }, { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "res": "p2.hp", "eq": 40 },
      { "sv": "p2.lastThrow", "eq": "scissors" },
    ] },
    { "signal": "duel.next" }, { "tick": 3 },

    // ── 第 4 回合：石赢了 ⇒ 它复用石 → 我回到布（规律闭环一圈）────────────
    { "signal": "charge.paper" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.paper" }, { "tick": 3 }, { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "res": "p2.hp", "eq": 20 },
      { "sv": "p2.lastThrow", "eq": "rock" },
    ] },
    { "signal": "duel.next" }, { "tick": 3 },

    // ── 第 5 回合：收 ─────────────────────────────────────────────────
    { "signal": "charge.scissors" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.scissors" }, { "tick": 3 }, { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "res": "p2.hp", "eq": 0 },              // 【R-108-15】血量归零
      { "flag": "p2.dead", "eq": true },        // 按侧判定（self-rule 读自身 → 各侧唯一旗）
      { "sv": "flow", "eq": "p1win" },          // **终局**：流程收束到胜利态（不等玩家点闸门）
      { "res": "p1.hp", "eq": 100 },            // 全程读对 ⇒ 完胜（复读机退化成随机就红）
    ] },
  ],
}
