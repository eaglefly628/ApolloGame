// GD-108 验收剧本 ③ · 满蓄被克（gdd §15 表第 5 行·验 R-108-14 + §6 风险曲线）**v3 重写**。
//   攒三个回合把石槽堆到 3 → 出石，对手出布 → 布克石：**自己**掉血，且石槽照样归 0
//   ——一次赔掉三回合的积累。这条守的是「公开蓄力」的代价面：蓄得越满越显眼，被读到就越亏。
// 对抗目标：败方不清零（那样满蓄零风险，整个心理博弈塌掉）→ 石槽断言红。
//
// ⚠ 2026-08-15 修（【R-108-35】）：第 2 回合从「出剪」改成「出布打平」。
//   原因是复读机的条款是「复用上一回合**赢过的**手」（gdd §9.4）——旧实现漏了"赢过的"三个字，
//   塌成"复用它自己上一手"，于是它一路蓄布，第 3 回合正好布槽 1。修好之后它蓄的是**赢家那只**：
//   前两回合都被我用剪赢，它就改蓄剪，布槽 0 ⇒ 这一笔只打 10，下面那条「伤害按出手方算」的
//   判据当场失去分辨力（按出手方算和按 base 算都是 10）。
//   故第 2 回合打成平局：平局那只手也算"没输过的手"，复读机于是在第 3 回合蓄布，判据复原。
//   平局不掉血（TIE_SELF_DAMAGE = 0·【R-108-15】），血量断言不受影响。
//   **这条改动本身也是条守卫**：复读机若退回"恒复自己上一手"，第 3 回合它蓄的就不是布，红。
{
  "name": "满蓄被克·赔掉三回合",
  "game": "game108", "seed": 108,
  "steps": [
    // 第 1 回合：蓄石但出剪（对手出布 ⇒ 我赢），石攒到 1
    { "signal": "charge.rock" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.scissors" }, { "signal": "throw.paper", "by": "p2" }, { "tick": 3 },
    { "tick": 35 }, { "tick": 92 }, { "signal": "duel.next" }, { "tick": 3 },
    // 第 2 回合：蓄石但出布 —— 与对手的布打平，把它「赢过的手」钉成布，石攒到 2
    { "signal": "charge.rock" }, { "tick": 2 }, { "tick": 275 },
    { "signal": "throw.paper" }, { "signal": "throw.paper", "by": "p2" }, { "tick": 3 },
    { "tick": 35 }, { "tick": 92 }, { "signal": "duel.next" }, { "tick": 3 },

    // 第 3 回合：石满 3 → 出石，被布克
    { "signal": "charge.rock" }, { "tick": 2 },
    { "expect": [ { "res": "p1.charge.rock", "eq": 3 } ] },
    { "tick": 275 },
    { "signal": "throw.rock" }, { "signal": "throw.paper", "by": "p2" }, { "tick": 3 },
    { "tick": 35 }, { "tick": 92 },
    { "expect": [
      { "sv": "flow", "eq": "settle" },
      // 【R-108-13】伤害 = 10 + **出手方**该手蓄力 ×10。对手不是木头：第 2 回合我跟它打平，
      // 复读机于是把力存进布（【R-108-35】蓄"赢过的手"），第 3 回合它布槽 1 ⇒ 打 20。
      // 这一笔正好也把「伤害按出手方算」钉死了——按我的槽算会是 40，按 base 算会是 10。
      { "res": "p1.hp", "eq": 80 },
      { "res": "p1.charge.rock", "eq": 0 },  // 【R-108-14】输了照样清零：三回合的力一次赔光
    ] },
  ],
}
