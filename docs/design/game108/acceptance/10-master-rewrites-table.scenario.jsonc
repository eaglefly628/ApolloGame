// GD-108 验收剧本 ⑩ · 大师自带改写过的判定表（gdd §15 表第 6 行同形状·验 R-108-32/40）。
//   Boss 的判定表被 patch 改过：**石同时克剪与布**。所以对他出布不再稳赢——
//   条款原话「Boss 强在规则，不强在血条」（双方一律 100）。
// 对抗目标：patches 不生效（大师用的还是标准表）→ 掉血的会变成对手 → 断言红。
{
  "name": "大师改表·石反克布",
  "game": "game108", "seed": 108,
  "config": { "opponent": "master" },
  "steps": [
    { "tick": 275 },   // 275 = T1 的 270 拍 + 定手窗 2 拍 + 2 拍余量（下限有点名测试守着）
    { "expect": [ { "sv": "flow", "eq": "throw" } ] },
    // **两只手都明写**：本剧本验的是**判定表**，不是大师的脑子。
    // v5 起大师会读牌反制（它这一回合的判读是「他会出石」⇒ 自己出布），
    // 靠"它默认出石"来凑这一局 = 尺子照着被测物做——AI 一改，表的测试就假红。
    // 代发 p2 的出招在 T2 里发，晚于它自己在定手窗的提交，接缝后写覆盖 ⇒ 这一手说了算。
    { "signal": "throw.paper" }, { "signal": "throw.rock", "by": "p2" },
    { "tick": 360 },
    { "expect": [
      { "res": "p1.hp", "eq": 90 },           // **玩家挨打**——规则被改写了
      { "res": "p2.hp", "eq": 100 },
    ] },
  ],
}
