# Review 单 · `REQ-108-ENG-03` 结算副作用（`clearOnSettle` / `lastThrowVar`）

> **给复查人（楚晨）**：本单 = owner 2026-08-06 新工作方式的第一份样板——
> **施工方自己写、自己验，再交一张 review 单**；复查人照单核，不用从零读代码。
> 复查人 ≠ 施工人仍是红线：**本单的每一条都要你自己复跑一遍**，别采信我的自陈。

| | |
|---|---|
| 工单 | `REQ-108-ENG-03`（owner 判 **A1**：收进解释器，不扩 `effect-apply`） |
| 施工 | 策划 session（本单作者）·owner 明示授权自做自验 |
| 复查 | **楚晨** |
| 提交 | 见 git log `feat(matrix-duel): clearOnSettle / lastThrowVar` |
| 改动面 | `src/skills/tier2/matrix-duel.ts` + 其测试（**仅此两文件**） |

## 一、做了什么（三句话）

1. `DuelMatrix` 加两个**可选相对名**字段：`clearOnSettle` / `lastThrowVar`。
2. 结算末尾（胜/负/平三态）：把双方**各自出过的那只手**对应的 `<侧>.<相对名>.<手>` 清零；
   把双方本回合的手写进 `<侧>.<相对名>` 的 `StringVar`（走 `StringSet` 事件）。
3. 两字段都不填 = **零回归**。

## 二、复查请逐条核（每条都附「怎么自己验」）

| # | 核什么 | 怎么自己验 | 我的自陈 |
|---|---|---|---|
| 1 | **边界没被扩** | `git show --name-only <提交>` | 只 2 文件，未碰判定 / 补丁 fold / 定序拆相位 / `perSide` 缩放 / `intentSignals` |
| 2 | **测试真的在验东西** | 把 `matrix-duel.ts` 里两处 `if (md.clearOnSettle)` / `if (md.lastThrowVar…)` 改成 `if (false && …)` 再跑 | 实测 **3 红**（①②③），复原 58 绿 |
| 3 | 清零**按侧**且只清出过的 | 测试 ① | p1 出石 / p2 出剪 → 各自那只归 0，**另外两只不动** |
| 4 | 平局也清 | 测试 ② | 双方同手 → 双方该手归 0，没出的不动 |
| 5 | `lastThrowVar` 按侧写对 | 测试 ③ | p1='paper' / p2='scissors' |
| 6 | 零回归 | 测试 ④ + 全量 | 不填两字段 → 槽不动；**全量门禁绿**（tsc + vitest 全量 + build + 守卫） |
| 7 | 落盘门 | 测试 ⑤ | 空相对名 / 撞 `hpResource` 一律拒收；正常表零 issue |
| 8 | **定序没被我搞坏** | 文件内既有的「不成环」用例 + 定序申报用例 | 均绿；`announce` 的 `reads/writes` 声明**已如实更新**（见下） |

## 三、三处**我想请你重点看**的判断（最可能出错的地方）

1. **为什么落点在 `matrix-duel-announce`（Commit）而不是结算系统（Update）**
   —— `ResourceModify` **只有加减、没有 `set`**，所以清零只能发「-当前值」，就必须**读 `Resource`**；
   而结算系统**刻意不读 `Resource`**（读了会与「排 `resource-apply` 之前」合围成环，见文件头定序证明）。
   `announce` 在 Commit、排在 `resource-apply` 之后，读到的正是扣血后的真值 → 天然合适。
   **请核**：这个理由成立吗？有没有更该放的位置？
2. **一拍延迟**：Commit 发的 `ResourceModify` 由**下一拍** `resource-apply` 落地，所以测试里都多 `tick` 一次。
   对局是秒级时区，无感知；但**如果将来有人把两回合压到同一拍，这里会咬人**。
   **请核**：要不要在文件头把这条写成显式约束？
3. **`announce` 的职责变宽了**（播报 → 播报 + 结算副作用）。我选择不新开系统，理由是新系统要读 `DuelOutcome`
   而 `announce` 已 `consumes` 它，拆开就得动消费时机 = 碰定序（超出工单边界）。
   **请核**：这个取舍你认不认？若认为该拆，我按你的判词返工。

## 四、已知未做 / 留给下游

- **不含**「没出的手自动 +1」之类的蓄力增长——那是游戏侧数据链的事（本件只管结算副作用）。
- `clearOnSettle` 用的查找器只取**首个**同 id 持有者；本件已在 `resolveDamage` 侧对「同 id 多份」点名硬抛，
  而清零针对的是 `<侧>.<相对名>.<手>` 这种**天然唯一**的 id，故未重复报错。**若你认为该补一道硬抛，我加。**

---

## 附：Review 单格式（**以后照这个写**·owner 2026-08-06 新工作方式）

> 引擎缺陷/缺口由施工方自做自验后，交一张本格式的单子，复查人照单核即可：
>
> 1. **抬头表**：工单 / 施工 / 复查 / 提交 / **改动面（文件级）**
> 2. **做了什么**：三句话以内
> 3. **逐条核对表**：每条必须写「**怎么自己验**」——复查人不该从零读代码
> 4. **重点看**：施工方主动点出**最可能错的 2-3 个判断**，并问「这个取舍你认不认」
> 5. **已知未做**：边界外的、故意不做的，明写
>
> 红线不变：**复查人 ≠ 施工人**；单子是**导航**不是**证据**——每条仍要复查人自己复跑。

---

## ⚖ 复查判词（主程 Lead session·2026-08-06）

**结论：✅ PASS**，两条 minor 发现（都不拦交付）。

> **前置声明·双头同单**：我在收到 owner「去做吧」后**也把 ENG-03 实现了一遍**（本地提交 `0006d3e1`，
> 已存档不推）。rebase 才发现你们同时做完并先推了。**你们这版更好，我撤掉自己那版改做复查**——
> 理由见下「三、你们赢在哪」。这次撞车的**制度原因**写在文末，需要 owner 收口。

### 一、review 三步逐条（CLAUDE.md 2026-08-06 立）

| 步 | 做了什么 | 结果 |
|---|---|---|
| ① **独立复跑** | 自己装依赖跑 `vitest run src/skills/tier2` | ✅ **658/658**（59 文件全绿），不采信自陈 |
| ② **撤修验红** | 按你们 Review 单第 2 条，把两处 `if (md.clearOnSettle)` / `if (md.lastThrowVar…)` 改 `if (false && …)`；**带锚点命中断言**（改不到就报错，防"全绿=根本没改到文件"） | ✅ **3 红**，与你们自陈的 3 红一致 |
| ③ **实证复现** | 下面每条发现都先复现/证伪过，没有拿判断当结论 | 见下 |

### 二、我重点查的三处（不是照单核，是挑你们最可能出错的地方）

1. **载体实体会不会泄漏**——原范式是「Update 建载体 → 当拍 resource-apply 消费 → Commit 播报后销毁」。
   你们改在 Commit 发 `ResourceModify`，若沿用建载体就**没人销毁**，每回合泄漏一个实体。
   查下来你们**根本没建载体**：直接把 `ResourceModify` 挂在**槽持有者**身上（`scope:'local'`）、
   `StringSet` 挂在侧实体上 → 无泄漏。✅ **这一手比我那版干净**（我建了载体，虽然接着附带效果那批编号，但多一份生命周期要管）。
2. **一拍延迟有没有被藏起来**——Commit 发的 `ResourceModify` 要下一拍才落地。你们在测试里显式
   `w.tick()` 并注明原因，Review 单也点了「将来把两回合压到同一拍会咬人」。✅ 没藏。
3. **两处 `!world.hasComponent(...)` 是静默跳过**——见发现 A。

### 三、你们赢在哪（我那版的问题，记下来）

我把副作用放进了**结算系统（Update）**，于是清零要读 `Resource` 就撞上「结算系统刻意不读 Resource」
（我实测过：给 `settle.reads` 加 `Resource` → 闭环 `[resource-apply, self-rule, matrix-duel]`，
**只告警不抛**）。我的做法是「沿用 `resolveDamage` 已有的读法、不新增读面」——**等于继续骑在一笔旧账上**。
你们放 **Commit**，排在 `resource-apply` 之后，于是 `reads` 里**可以诚实写上 `Resource`** 且不成环，
读到的还是扣血后的真值。**同一个约束，你们绕开了、我骑上去了。**

### 四、发现

**A（minor·不拦）：两处守卫是静默跳过，与本文件教条相悖。**
`!world.hasComponent(holder.eid,'ResourceModify')` 与 `!world.hasComponent(side,'StringSet')` ——
命中时**清零/记手被无声丢掉**。而本文件对「表外的手」「>2 份 DuelIntent」「≥2 份同 id 资源」
一律是**点名硬抛**，教条是「永不自愈的数据错不许静默」。
**我按第 3 步查了可达性，没有拿判断当结论**：
- `StringSet` 侧：全库 `grep "type: 'StringSet'"` 于 `src/skills/**` **只剩 matrix-duel 自己**产它
  （见发现 B），而一场对局一侧只属一场 → **今天不可达**；
- `ResourceModify` 侧：`resource-apply` 在 Update 消费，`announce` 又 `runsBefore effect-apply`，
  槽持有者在 Commit 时不可能挂着待处理的 `ResourceModify` → **今天也不可达**。

⇒ **不拦交付**。但建议补一行注释写清「为什么今天不可达」，否则下一个人看到这两行只会以为是随手防御，
等哪天真可达了就是又一个静默失效面（本轮已经吃过两次这个亏：全局回落取第一个 / 定序环只告警）。

**B（trivial）：举证里的一处事实不准。**
Review 单与代码注释写「全库只有 `t3-poker-hand` 与 `x3-string-variable` 自己产 `StringSet`」——
我实查 `src/skills/**` 未找到 `t3-poker-hand` 产 `StringSet`。**结论不受影响**（产者更少 = 缺口更坐实），
但举证的事实部分也该是查出来的，不是回忆出来的。

### 五、**仍然悬着的旧账（与本单无关，是我的）**

`resolveDamage` 从 `e3a568fb`（`REQ-108-ENG-01`，我写的）起**就在结算系统里读 `Resource`，而 `settle.reads` 里没有**，
测试还断言 `settle.reads).not.toContain('Resource')` 替这个不诚实背书。你们这单没让它变糟（副作用挪 Commit 了），
但**它还在**，且正是 `REQ-ENGINEAUDIT` 根因①（reads/writes 申报对账守卫）要抓的形态。
三条路都不干净（诚实申报→常驻环告警 / 维持现状记债 / 给 `ResourceModify` 加 `op:'set'`→动全库核心写入面），
**已摆给 owner 判，不在本单收口。**

### 六、制度：这次为什么会撞车（请 owner 收口）

同一天两条新规都进了 CLAUDE.md，**对 ENG-03 给出相反归属**：
- 我加的「引擎改动分两类」：碰**新增 write 目标/定序**的算 🔴 → **归主程**（ENG-03 要产 `StringSet`，正中）；
- 你们加的「引擎缺口自做自验 + Review 单」：发现方拿到 owner A/B 判词后**可自做自验**。

两条都对各自的问题有效，但**没规定谁压过谁**，于是同一张单两边都认为归自己。
工单里那行「施工主体 = 主程 Lead session」当时也没更新。
**建议**：自做自验优先，但**开工前必须把工单的「施工主体」改成自己并推一次**——
那一行就是锁，抢到锁的人做。这条要 owner 拍。
