# game108《拳律 / Rule of Three》· 游戏级需求单

> 本单**不占引擎池硬槽**（`docs/workflow/requests.md` 的 10 槽铁律）。工单随游戏走。
> 引擎面改动条目在此登记 = **Lead 已裁但因引擎池满槽而降级存放**，派工时照本单的写死 spec 施工，边界不得扩。

## 待处理

### ~~PLAYTEST-108-0815~~ · owner 试玩两条：前四档 AI 恒出石 + 首次进入没有玩法说明 · [2026-08-15] · status: **✅ done（本提交）** · 类型: 试玩返修
> **原话**：「① 我们刚出来的时候是要先跳一下玩法说明。如果说玩家可以选跳过，这还是要有的。
> ② 它这个 AI 现在**永远只会出锤子**、榔头、石头，我不知道为什么，不是我们 AI 已经实践完了吗？」
>
> **② 实测比报的更糟**：五档里**四档**六回合全是石头（只有第五档大师 v5 会变），
> 而默认卡 `mood: stubborn` 打的正是第一档复读机 —— owner 从头到尾打的就是那个恒石对手。
> **根因是输入选错、不是规则写错**：①③④ 读的都是 `p2.lastThrow` = **它自己**的上一手，
> 那是个不动点（开局空串 → 兜底出石 → 恒为石 → 再读还是石，**从第一回合锁死**）。
> 大师会变，只因为它读的是**玩家**。gdd §9.4 条款原文是「复用上一回合**赢过的**手」——
> 旧实现漏的正是"赢过的"三个字。修法：**前四档一律改读会变的输入**（玩家的手 / 上一回合的胜负 /
> 种子骰），并补 `aiDieRules()` 走引擎种子 PRNG（游戏层禁裸 `Math.random`）。
>
> ⚠ **④ 赌徒与 gdd 表「血少时必押满蓄」有意偏差**：那句要读**它自己**的血，而
> `ConditionExpr{resource}` 是全局 id 路由、两侧 hp 同 id ⇒ 全局只读得到 p1 那份；
> p2 想读自己的血只能用它那格 `SelfRule`，已给了死亡判定（一实体一组件挤不下第二条）。
> 按卡片化之后的心情语义「阴晴不定·忽冷忽热」落成骰子，代码里留痕。
>
> **这一条为什么活了半个月**：四道复查门 + 十二本验收剧本 + 浏览器通关**全绿地放它过了**——
> 因为没有一条测试钉过「AI 会换手」，而剧本 ⑦「五回合都出布打死复读机」**把 bug 写成了验收基准**
> （那条打法成立的前提正是"复读机恒出石"）。**手册守得住「改坏了会红」，守不住「从来就没对过」。**
> 补网：单测 `【R-108-35】` 六条（含"没有一档是恒出石"、逐档条款、防空转），三处撤修各自即红；
> 剧本 ③⑦ 改按条款重写（⑦ 现在是「读出它的规律 → 连赢五回合」，它一退回恒石就红）；
> 旅程脚本改成**读着规律打**（每回合推它下一手并断言推准），41/41 绿。
>
> **① 玩法说明**：加载 → PRESS ANY KEY → **说明屏（「跳过 · 开始」）** → 对局。
> `helpSeen` 落 localStorage（宿主侧·不进世界/hash/lockstep，同 `lang` 那条分界），看过就不再弹。
> 说明屏压在开始屏**之上** —— 盖反了就是"点不到跳过、零报错卡死"，单测 + 旅程 `elementFromPoint` 各守一道。


### REQ-108-GD-03 · 验收剧本裸拍数迁移 `waitUntil` · [2026-08-09] · 引擎已落 REQ-WAITUNTIL（owner 判 A·game108 复盘带出） · status: open · 指派：GD-108 · 优先级: P2（非阻塞·下次调节奏时顺手做） · 类型: 剧本维护
> **引擎侧已就绪**：剧本步骤新增 `{"waitUntil": [断言…], "cap": N}`——断言复用 expect 闭集、先查后拍、封顶 FAIL 带已等拍数（口径见 `docs/playbooks/testing.md` 验收剧本节）。**裸 `{"tick":N}` 仍合法**，本单非强制重写。
> **为什么是你家的单**：十二本剧本 `"tick"` 共 103 处写死拍数（如 `{"tick": 275}` 等结算）——节奏一调全要人肉重算，正是本次复盘认定的痛点源头。剧本作者=GD 铁律，主程不代改（污染 blame·S4 复查门有「剧本作者非 PE」检查项）。
> **做法**：「等状态成立」类的等待（等 REVEAL/等结算/等 AI 出手）改 `waitUntil`（等的目标写状态断言·cap 给宽裕上限）；纯节拍推进（如「过 3 拍再充能」的规则性拍数）**保留裸 tick**——那是规则本身，不是等待。改完 `ZEROCRAFT_ACCEPTANCE_CLI=1 npx vite-node scripts/acceptance-run.mjs --game game108` 全绿再推。

### REVIEW-108-S7 · S7 品质关评分卡待复查人打分 · [2026-08-08] · status: **待复查人接单** · 优先级: P1 · 类型: 复查门（S7 的机器门 = 评分卡本身）
> **谁打**：S7 的机器门就是评分卡，而评分卡**由复查人执行**（`game-pipeline.mjs:234`）。
> 施工 session 不能给自己打分——同 S2–S5 那条红线。
> **回执**：`docs/design/game108/self-check/S7-scorecard-selfassess.md`
> —— 逐维证据 + 我的**自评 15/24 · PREMIUM: NO**（短板 = 主角面 / 世界密度）。
> **口径**：owner 2026-08-08 判 A，手册 §二 现在每维两条底线，本作按 **2D 线** 那一列打。
> **最该被对抗的一处**：4/5 两维（材质 / 渲染管线）的 2D 底线**是我写的**，我又给自己打了 2 分。
> 若你认为那是「把底线写得刚好够自己够到」，直接推翻——判据与被测物同源是本轮最大的自我怀疑点。
> **另一件人门事**：`golden/boot.png` 是 candidate 未 blessed（bless = 人门语义·note 必填）。
> **落账**：`node scripts/game-pipeline.mjs scorecard game108 --scores "艺术方向:N,主角面:N,世界密度:N,材质:N,渲染管线:N,VFX:N,UI美术:N,性能证据:N" --by 复查人 --note "逐维证据摘要"`

---

### REVIEW-108-S2S5（**owner 2026-08-08 指派：主程要查**·非引擎面·不占引擎槽） · game108 四道复查门（S2/S3/S4/S5）一并复查 · [2026-08-08] · status: **待主程接单** · 优先级: P1 · 类型: 复查门（三门制的第二门）
> **背景**：S3/S4 的机器门与人门都绿了（owner 2026-08-08 口头签），**复查门四道全空**。
> 复查人 ≠ 施工人是红线，施工 session（写 GDD/blueprint/UI 的这个）不能自查。
> **接单第一命令**：`node scripts/game-pipeline.mjs checklist game108 S2`（S3/S4/S5 同）。
> **导航单**：`docs/design/game108/review/REVIEW-S2-S5.md` —— 含改动面、声明边界、
> 十分钟机器证据一组、逐关逐条「怎么自己验」、**八个撤修锚点**（撤掉应各红一条）、
> 以及施工方自判的三处 CONCERNS（capability-plan 未跟上 v5 / 递归复核未重跑 / `check-ui` 未跑新屏）。
> ⚠ **导航单是导航不是证据**：每条仍须复查人自己复跑，别采信施工方自陈那一列。
> **范围**：`ec6794a9`…`1f98741d` 的 game108 面（中间两笔 game211 与本单无关）。
> **落账**：`node scripts/game-pipeline.mjs review game108 <SN> --verdict … --note … --by 主程`

---


### ~~REQ-108-ENG-01~~ 收益缩放（`DuelPayoff.damage` 按资源线性缩放·`perSide` 相对名） · **✅ 已交付并验收** · 全文（缺口举证 / owner 判 A / 返工 spec / 复查改判 / 三条教训）查 git 历史：`git log -S REQ-108-ENG-01 -- docs/design/game108/requests.md`

### ~~REQ-108-ENG-02~~ 出招输入接缝（`intentSignals` + `EventWhen.source` 认侧） · **✅ 已交付并验收** · 全文（含「Commit 相位闭环成环」那条定序地雷的实测原文）查 git 历史：`git log -S REQ-108-ENG-02 -- docs/design/game108/requests.md`

### ~~REQ-108-ENG-03~~ 结算副作用（`clearOnSettle` / `lastThrowVar`） · **✅ 已交付并验收**（Review 单 `review/REQ-108-ENG-03.md` 仍在档） · 全文查 git 历史：`git log -S REQ-108-ENG-03 -- docs/design/game108/requests.md`

### REQ-108-UI-03 · 面板缺「平移投影」（卡通立体感的基件）· [2026-08-07] · owner 当场指出 · status: **✅ 已下沉（PUI·判 A）· 游戏侧暂不接（owner 定·见下）**

- **病**：owner 看图第一句——「你 / 蓄力 / 复读机 这三个牌**不是 3D 浮空的**，下面该有颜色和阴影」。
  设计定稿里这是全屏统一的语言：`box-shadow: 0 4px 0 #14776a`（身份牌）/ `0 5px 0`（相位牌）/
  `0 7px 0`（招式卡）/ `0 8px 0 #c9932a`（主 CTA）——**硬边偏移投影**，不是模糊阴影，
  卡通 UI 的「浮空感」全靠它。
- **查过了**：`PanelProps` 有 `bg / edge / accent / dashed / radius / shape / skin / glass / vignette`，
  **没有任何投影字段**；`layout.press3d/tilt3d` 是交互态（:active/:hover）不是常态投影；
  `accent` 那条 `box-shadow` 是固定柔光不可配。→ 现有闭集**表达不了**。
- **要做的**：`PanelProps.shadow?: { y: number; color?: SurfaceToken | string }`
  → 渲成 `box-shadow: 0 <y>px 0 <color>`。默认不填 = 零变化（全库老屏字节不变）。
  同类问题一并考虑（可分期）：`LabelProps` 的硬边文字投影（`text-shadow: 0 5px 0 <ink>`，
  稿子的伤害数字就用这个；我们现在只有 `stroke`/`glow`，出不来那个"厚"）。
- **⚖ owner 2026-08-07 明确的边界**：「**我们做归做，但选择吃贴图可能也是我们最终选择**」——
  所以这条单子**只加字段，不废弃贴图路线**：`Panel.skin` 仍是有真美术时的正解，
  两条并存不互斥（`shadow` 给"程序化面"，`skin` 给"画好的面"）。落地时**不许**顺手把
  game108 的 `plate-art.ts` 删掉或标为过时——那是另一个决定，等真美术到位再议。
- **验收**：`Panel{shadow:{y:4,color:'#14776a'}}` 渲出硬边偏移投影；game108 可选择性地把
  `plate-art.ts` 的投影那一支换成字段（**换不换由 game108 自己定**）；老屏零回归。
- **判据（为什么值得下沉）**：那把尺子——`shadow:{y:4}` 最弱的模型也填得出，
  「去写个 SVG 生成器」填不出。且 game-i 糖果钮、apollo-toon 厚唇皮都在手搓同一件事。

### REQ-108-UI-06 · 补一款**圆润数字艺术字**槽（Fredoka）· [2026-08-07] · **owner 自发给 PUI** · status: **✅ 已加槽（PUI·`round`）·待游戏侧接**

- **病**：设计定稿的数字（血量 / 倒计时 / `n/3` 读数 / **伤害大数**）全部指定 **Fredoka**（500/600/700），
  它是与中文那款 ZCOOL KuaiLe 配套的圆润无衬线，两者一起构成稿子的"卡通"底色。
- **查过了**：`Label.font` 的 18 款拉丁艺术字里，圆润一路只有 **`bubbly`(Baloo 2)**。
  Baloo 2 与 Fredoka 同属圆润无衬线、也有真 500/600/700 字重，**当前就是用它顶的**，
  观感接近但不是同一款——字碗更圆、数字 `4`/`7` 的骨架不同，大字号（伤害数 140px）下看得出来。
- **要做的**：照 `cnround` 那次的既有产线加**第 19 款拉丁艺术字槽**（如 `round`）：
  Fredoka 是 **SIL OFL**（Google Fonts），与现有 18 款同许可、同 `art-fonts.ts` 内嵌管线。
  需要 500/600/700 三个字重（稿子三档都用到）。
- **不做也能交付**：`bubbly` 是可用替身，这条是**观感精修**（S5 面），不阻塞任何门。
- **验收**：`Label{font:'round', bold:true}` 渲出 Fredoka 700；game108 把 `design-tokens.ts`
  的 `F.num` 从 `'bubbly'` 切过去，与稿子 `screens/*.png` 的数字并排看骨架一致。

> **✅ PUI 交付（2026-08-07·加槽 `round`）**：Fredoka（SIL OFL·**可变字体** wght 300–700）。
> **两处与工单假设不同的工程判断**（都往省成本/不改语义走）：
> ① **走 url() 惰性载而非 base64 常驻**——拉丁 18 款是 base64 塞进 `art-fonts.ts`（584KB·**全游戏常驻加载**）；
>    Fredoka 3 字重塞那儿等于给每个游戏的首屏加税，而它只 game108 用。故复用 CJK 的惰性管线
>    （`cjk-art-font-vendor.py` 加一条 → `round.woff2` 34KB·主 bundle 零增·只在真渲染时拉）。
> ② **一颗可变字体覆全字重·不加 `Label` 字重字段**——`@font-face font-weight:300 700`（保 wght 轴）；
>    `Label` 现只发 400/700（`bold`），game108 实际也只走 `bold`→700。真要 500/600 得给 `Label` 加闭集
>    字重字段，game108 不消费 → YAGNI，不做。可变字体已让 bold→700 天然取到，将来加字段也无需重打包。
> 落点：`cjk-art-font-vendor.py`(FONTS 加 Fredoka·支持字重范围 + 可变字体 name 表保留) · `art-fonts.ts`
> `ART_FONT_FAMILY.round='Fredoka'` · `types.ts`/`catalog.ts` 枚举 + 文档 · `label-font-glow.test.ts`
> (round→Fredoka·bold→700·@font-face 覆 `font-weight:300 700`) · game-i 展台加 `font:round` 数字墙（vs bubbly 对照）。
> 真渲染目击：`140` 大数 + `0123456789` Fredoka 骨架（与 bubbly 并排可辨）·汉字正确回退主字体。`scoped-gate` scope=full 全绿。
> **游戏侧接法**：`design-tokens.ts` 的 `F.num` 从 `'bubbly'` 改 `'round'`（`bold:true` 取 700）。

> **✅ PUI 交付（2026-08-07·判 A·下沉）**：`PanelProps.shadow?: { y: number; color?: SurfaceToken | 色串 }`
> → 渲 `box-shadow:0 <y>px 0 <color>`（硬边非模糊）。`color` 缺省=深墨(`ink`/`bg0`)·优先填闭集 `SurfaceToken`
> （换皮自适应）·裸色串走稿子精确墨色。与 `accent` 柔光**逗号共存**、作用于 skin 框皮面（`plate-art` 可删投影支）。
> 非法 `y` 走 `num()` 退化 0px·绝不 NaN。真渲染目击 4 档（y=4/5/7/8 浮空梯度）+ 对照无投影已过。
> 落点：`types.ts`(字段) · `render.ts`(`shadowColor` 解析 + box-shadow 叠层合成) · `catalog.ts`(spec) ·
> `panel-fill.test.ts`(+8 例·含"验收核心 y 偏移"/换皮/裸串/accent 叠/skin/bare 忽略/NaN 退化/零回归) ·
> game-i 展台加 `PANEL.shadow` 货架行。`scoped-gate` scope=full 全绿。
> **游戏侧接法**：`design-tokens.ts` 身份牌 `shadow:{y:4}` / 相位牌 `{y:5}` / 招式卡 `{y:7}` / 主 CTA `{y:8}`，
> 颜色填 `SurfaceToken`（换皮）或稿子墨色。**切不切、删不删 `plate-art.ts` 由 game108 定——见下方 owner 裁（贴图路线不废弃·本轮不切）。**

> **⚖ owner 2026-08-07 裁的边界（压过上面那句「接上后删 plate-art.ts」）**：
> 原话——「**我们做归做，但是选择吃贴图可能也是我们最终选择**」。故：
> ① 字段**照做**（已 done，全库受益，与本条无冲突）；
> ② **`skin` 贴图路线不废弃**——两条并存不互斥：`shadow` 服务「程序化面」，`skin` 服务「画好的面」；
> ③ **game108 本轮不切**。理由不是懒：这一屏的皮里烤的不只是投影，还有面渐变 / 墨边 / 顶部色条 /
>    底部副标条 / 已提交外描边 / 蓄满发光圈——只把投影那一支挪出去，会变成「投影走字段、
>    其余仍走皮」的双轨，还得把每张皮的高度按 `sh` 重算一遍。**换来零视觉收益，却引入一轮回归风险**，
>    而我们刚把这屏对到 1:1。真美术贴图到位那天再整体重估（那时可能连面渐变都由真图承担，
>    正好落到 owner 说的"吃贴图"那一头）。
> ④ 谁都不许因为「字段有了」就顺手删 `games/game108/plate-art.ts`。

### REQ-108-UI-04 · 缺一款**卡通粗中文**字体槽 · [2026-08-07] · owner 当场指出 · status: **✅ 已加槽（PUI）·待游戏侧接**

- **病**：owner——「字体不够饱满，要一定的艺术粗体」。设计定稿指定 **ZCOOL KuaiLe（站酷快乐体）**
  作为全屏中文显示字，Fredoka 作为数字字。
- **查过了**：`Label.font` 的 CJK 艺术字槽只有四款——`cnbrush`(马善政毛笔行楷) / `cnwen`(站酷小薇文艺细宋) /
  `jpbrush`(筑紫日文毛筆) / `jppen`(Klee One 日文楷書)。**三款是毛笔/楷体、一款是细宋，没有一款是粗卡通体**。
  拉丁侧 `bubbly`(Baloo 2) 与 Fredoka 同族圆润，数字这一半已经能顶上（现用它）。
- **现在的做法**：中文走主字体 `ui` + `bold:true`。字重顶上来了，**字形还是系统黑体，不是卡通体**——
  与稿子的观感差在这一处，是本次复刻**最大的单项视觉偏差**。
- **一条路就够（不构成 A/B）**：ZCOOL KuaiLe 是 **SIL OFL**，与现有四款 CJK 艺术字同一许可、同一条产线——
  `scripts/cjk-art-font-vendor.py` 子集化 + `art-fonts-cjk.ts` 加一个枚举值（如 `cnkuai`）。
  这是**加一个槽**，不是放宽闭集，与手册「缺字体→提 requests 让主程加一个槽」的口径完全一致。
- **验收**：`Label{font:'cnkuai'}` 能渲汉字；game108 把 `design-tokens.ts` 的 `F.cjk` 从 `'ui'` 改成它，
  与稿子 `screens/*.png` 并排看字形一致。

> **✅ PUI 交付（2026-08-07·加槽）**：槽名落定 **`cnround`**（非建议的 `cnkuai`——与既有 `cnbrush/cnwen` 的
> "语种+风格"命名一致：cn+round 圆黑）。ZCOOL KuaiLe（站酷快乐体·SIL OFL）走既有 CJK 惰性载产线：
> `cjk-art-font-vendor.py` 子集化 → `cnround.woff2`（url() @font-face·非 base64·主 bundle 零增）→
> `ART_FONT_FAMILY.cnround='ZCOOL KuaiLe'`·render 自带 `'ZCOOL KuaiLe','PingFang SC'…` 兜底链。
> `types.ts`/`catalog.ts` 枚举补齐(4→5 款)·`label-font-glow.test.ts` 点名验·game-i CJK 字体墙加样张·真渲染目击。
> **游戏侧接法**：`design-tokens.ts` 的 `F.cjk` 从 `'ui'` 改 `'cnround'`。若稿子出现 src 从未用过的生僻字→
> 重跑 `python3 scripts/cjk-art-font-vendor.py` 重新子集（幂等）。**⚠ 但 vendor 只扫 `src/**` → 游戏层文案缺字见 UI-05。**

### REQ-108-UI-05 · CJK 字体子集只扫 `src/**`，游戏层文案会逐字静默回退 · [2026-08-07] · status: **✅ 已修（PUI·glob 加 games/**）**

- **病**：`scripts/cjk-art-font-vendor.py` 的子集字符集来自 `src/**/*.ts(x)`（脚本第 31 行的 glob），
  **不含 `games/**`**。游戏层的文案只要用到一个 src 里没出现过的汉字，那个字就不在 woff2 里，
  浏览器**逐字**回退系统字——同一行里混两种字形，**不报错、不告警**。
- **实测**（game108 切到 `cnround` 时顺手量的）：本作屏上会出现的 91 个汉字里缺 3 个——
  `莽` `夫` `徒`，来自对手名「莽夫」「赌徒」（当前只上场「复读机」，所以这一版看不出来，
  等五名对手轮换上场就会露）。
- **建议**：glob 加一行 `games/**/*.ts(x)`（子集会略大，但 CJK 子集本来就是按字取，增量很小）；
  或更稳的做法是加一道**覆盖率检查**：扫 src+games 的汉字集，与生成的 woff2 cmap 对账，缺字报警。
- **为什么不是我改**：`scripts/cjk-art-font-vendor.py` + `art-fonts-cjk.ts` 属 PUI 域（UI 基座），
  按域边界报单不擅改。
- **绕过**：无需绕过——缺的 3 个字当前不上屏；上屏前 PUI 重跑一次 vendor 即可。

> **✅ PUI 交付（2026-08-07·采纳建议一「glob 加 games/**」）**：`char_set()` 的 glob 从 `src/**` 扩到
> `src/** + games/**`（`.ts`/`.tsx`）→ 重跑 vendor。子集 3275 字（含游戏文案）·五款 woff2 重新子集
> （url() 惰性载·主 bundle 零增不变）。实测 `莽/夫/徒` 现已进 `cnround.woff2` cmap（fontTools 直核·三字 True）。
> 未采建议二（覆盖率对账告警）——glob 扩容已直接消灭静默回退，对账是二次防线·YAGNI。
> **注意**：游戏文案改动后若引入 src+games 都没出现过的新汉字，仍需 PUI 重跑 vendor（幂等）——这是子集化的
> 固有性质·非本次遗留（真要免维护得走"全字库不子集"·体积代价不划算）。

### REQ-108-UI-07 · i18n 到了「第三例」触发线 · [2026-08-07] · status: open（Lead 裁·抽不抽）

- **实查**：引擎**无 i18n 能力**（`src/` 零 locale 服务，无任何切换机制）。既有两例都是游戏层自持字典：
  `games/game-a/strings.ts`、game-c。game-a 的文件头自己写着触发线——
  「本游戏自持字典即可，**不下沉引擎**（YAGNI·game-c 已是第二例·**第三个游戏要再抽共用能力**）」。
- **现状**：game108 = **第三例**（`games/game108/strings.ts`，照同一模式落的：`{en,zh}` 数据 + `t(lang,key)`
  + localStorage 持久化）。三份字典的**解释器部分逐字相同**，只有词条不同。
- **两条路（Lead 裁）**：
  - **A 抽成共用能力**：`src/ui/i18n.ts` 出 `Lang / makeDict(entries) / t / loadLang / saveLang`，
    三个游戏各自只留词条表。代价：动 `src/**` + 三个游戏迁移；收益：解释器一份、口径一致，
    第四个游戏零成本。
  - **B 维持各自持有**：三份各活各的。代价：解释器复制到第三份，往后每加一个游戏再抄一次；
    收益：零迁移风险。
  - **推荐 A**，但**不是现在**：三份实现细节已有分叉（game-a 有 `money()` 千分位、本作有 `CHAR_W`
    排版系数），先抽会把游戏专属的东西一起抽上去。**建议等第四例出现时连同分叉一起收**——
    这不是拖，是让"共性"先长够。
- **本轮不阻塞**：game108 已按现成模式交付，中英双版可跑。

### REQ-108-GD-01 · 定位变更：**约会向 + 两种模式**（对手 = 传入卡片角色）· [2026-08-07] · owner 定方向 · status: **待 owner 补齐决策点**

- **owner 原话**（2026-08-07）：
  「对手是我们传进来的**卡片角色**……不叫敌人，是自己的**约会对象**」
  「跟这个人比赛，不是去冲哪个关或 BOSS，而是**他的情绪是什么、他今天的心情是什么**」
  「**卡片的心情就是它的 AI**」
  「所以我们**两种模式都要做**：一种要有点剧情；一种是跟卡片人物娱乐对战。
   还有一种是他跟你说一句对白，然后说『我们来挑战一下我们设定的这些 AI』——这时候模式就变成 **VS AI**，叫**猜拳机**」
- **对既有条款的冲击**：【R-108-32】「五名对手 = 一条教学曲线」**语义作废**。
  五套出招规律本身**一个不浪费**——它们从"五个关卡"改成**五种心情的行为实现**
  （`games/game108/card-character.ts MOOD_AI`：执拗→复读机 / 上头→莽夫 / 玩心→戏子 /
  阴晴不定→赌徒 / 精明→大师）。**换心情 = 改一个字段，不写代码。**
- **本轮已落地的部分**（不必再议）：
  - 对手的**名字与画像来自卡片**（顶栏身份牌 + 对手蓄力条标签都取 `card.name`；画像有图贴图、无图退化成名字首字）；
  - **心情 → AI** 查表 + `setCard()` 装载口（`mount(el, host?)` 的第二位被 launcher 占用，故走装载前设值）；
  - 中英双语（`strings.ts`）+ 屏上语言胶囊。
- **⚠ 还差的四个决策点（owner 域·我不自裁）**：
  1. **一局的意义**。不冲关了，那赢一局得到什么？（好感 +N？解锁对白？纯娱乐无产出？）
     没有这个，"再来一局"就只是重复劳动——S4 自证里「重玩性是假的」那条至今没解。
  2. **模式 A（剧情向）的剧本从哪来**。库里已有 `dialogue` 能力 + `@ui/starters/presence`（反应表 + 确定性选句），
     接得上；但**台词内容是 GD 的活**，且要不要随心情/胜负分支，得先定。
  3. **模式 B（VS AI 猜拳机）的入口与规则**。owner 描述是「卡片角色说一句对白 → 转成挑战内置 AI」。
     那这时对手换成谁？五种心情轮着打？有排行/连胜吗？
  4. ~~**卡片契约**没有~~ → **✅ 我说错了，已更正并接上（2026-08-07）**：
     契约**一直都在**——`src/services/character-card/`（`normalizeCharacterCard` / `toSeatCard` / `isCardUsable`），
     手册 `docs/playbooks/character-card.md`，a/b/c 三个游戏已在用。
     我先前只查了 REQ-DIALOGUE 里那段被悬置的「DokiWorld 数值双向契约」就下了"没有"的结论，**漏查了 services 层**。
     现已按手册接：`fromPlatformCard(draft, mood, opts)` → 桥收敛 name/id、画像取 `media.avatarUrl`（退 `imageUrl`）。
     **唯一平台卡里没有的是 `mood`**——那是「他**今天**的心情」，是**本次对局的参数**不是角色属性
     （同一个人今天上头、明天精明），故由会话侧传入。
     待定的只剩：接入时要不要开**成年硬闸** `requireAdult`（a/b/c 姨太题材是必开的；本作约会向非成人向，
     默认没开）——**这条归 owner 按题材定**。
- **菜单**：owner 要求「菜单里加多语言设定」——**game108 目前没有菜单屏**（装载即进对局）。
  本轮把语言开关做成屏上一枚自描述胶囊（齿轮正下方）先能用；**真设置面板随模式选择屏一起做**
  （两件事共用同一个"局外屏"，分两次做等于搭两遍）。

### REQ-108-AUDIO-01 · 音频与配音已接（记账·非缺口）· [2026-08-07] · owner 要求 · status: **done（本轮）**

owner：「我们游戏有没有音乐？给我做一下 AI 的配音。」——**先查手册再动手**（`docs/playbooks/audio.md`），
三样能力全是现成基座，一个都没自造：

| 要的 | 用的基座 | 落点 |
|---|---|---|
| 音效 | `SynthAudioPort` + `SfxSpec`（**声音 = 数据**·音色表） | `games/game108/audio.ts` · 11 个事件键 |
| 背景音乐 | `SynthMusicPort` + `MusicTrack`（音符表·循环合成） | 同上 · I–V–vi–IV 轻柔垫底（约会向：不抢戏） |
| **角色配音** | `VoicePort` 降级链：`TtsVoicePort`（浏览器合成·零资产零 key）→ 采样包（将来真配音）→ **字幕兜底** | `games/game108/voice.ts` |

- **零外部音频文件**、确定性、无 `AudioContext`（headless/探针/SSR）自动静默 no-op —— 手册红线全守。
- 配音是**表现层旁路**：只在"世界已经变成这样"之后触发，**绝不进 sim / hash / 录放 / lockstep**。
- 台词是数据表（`事件 × 语言`），语气按**卡片的心情**取 `rate/pitch`；内容将来由 GD 按人设改写，**改表不改代码**。
- 兜底：TTS 发不出声（没装音色 / headless）时照样打**字幕**——听不见也要看得见他说了什么。
- **待补**：真人/真 AI 配音的采样包（`SamplePackVoicePort` 那一档），等有音源再挂；台词内容待 GD。

### REQ-108-GD-02 · v3 节奏案（owner 2026-08-07 口述）· 可行性预判 · status: **待 owner 签策划案 + 定 7 个数**

owner 明确：**先落策划案，通过了再实现**。策划案已写进 `gdd.md` §3 / §3b / §5b（v3）。
本条只记 Lead 侧的**可行性实查**，让 owner 签的时候知道每一条要付什么。

**一、能力上都够得着（实查过，不是印象）**

| v3 要的 | 用现成的什么 | 备注 |
|---|---|---|
| T2 超时**每 N 秒扣 M 血** | `over-time`（`src/skills/tier3/over-time.ts`·`TimedEffect{amountPerTick, period}`） | 正是"按周期扣资源"，纯整数 tick 计数、进 snapshot 可重放 |
| T4 **玩家点「下一轮」**才推进 | `FlowTransition.when`（条件树）+ 一个信号置旗 | `logic.ts:193`；`after` 还能给"无操作 X 秒自动进"的兜底 |
| T2 **免费时间**后才开始罚 | `FlowTransition.after`（进入状态满 N tick 才允许转移） | 同上 |
| 粒子**从卡飞进槽** | `layout.flyTo{to,ms,arc}`（元素沿弧飞到目标元素 id） | 基座件·`ui.md` 在案 |
| 伤害数字**跳数** | `Label.tween{from,to,ms,decimals}` | `types.ts:198` |
| 卡面**从下往上注色** | 生成图（`plate-art.ts` 已能画渐变）+ 按相位时钟算填充高度 | 与手的动作同一条路子（相位时钟驱动·非 CSS 关键帧） |
| **震动幅度随掉血量** | 同上：算出位移填 `layout.x/y` | 闭集 `anim:'shake'` 幅度写死 ±4px，**表达不了"幅度可变"**；用算的那条路绕开，不必开引擎单 |
| 血槽**双段条**（延迟追赶） | 两张生成图叠加 + 宿主保留上一帧血量 | 纯表现·不进 sim |

**→ 结论：v3 不需要任何新的引擎能力。** 全部落在"游戏层数据 + 现成基座"里。

**二、要 owner 认的三笔代价**

1. **超休闲的「一局 60–90 秒」承诺没了**（T4 玩家闸门 + T2 软超时）。要么放弃这条，要么给 T4 自动兜底。
2. **【R-108-02】被反写**：v2 是「超时不罚玩家、不卡节奏」，v3 是「罚，而且卡到你出手」。方向变了，不是调参。
3. **【R-108-08】T1 整屏放大 ↔ 【R-108-03】六条槽全程可读**：T1 正是对手在存力那一拍，
   整屏盖住 = 在最关键的时刻把本作**唯一支柱**关掉。已在 GDD 里摆了 A/B/C 三条路，**推荐 A**
   （放大到中央通道——横版正好把那条道留出来了，顶栏与右上角对手条照常可见）。

**三、连带要改的**

- 验收剧本（10 条）里凡是依赖"固定 3/3/2/1"的时序假设都要重写——**剧本作者 = GD，PE 不许改**。
- 试玩走查 35 条断言里的相位等待逻辑靠读屏上相位名，不写死秒数，**大部分能活下来**；
  但"T2 出招当下不掉血"这条会被罚血打破（罚血也掉血），需要区分"罚血"与"战果掉血"。
- `PHASE_TICKS` 从四个常数变成"三个时长 + 两个闸门条件"，`theme.ts` 的单一真相要跟着改形。

### REQ-108-GD-01 · 验收剧本动作覆盖缺口：GDD §14 声明 10 动作·剧本只练到 6 · [2026-08-08] · ui-brief 推导带出（brief-2026-08-08 实测）→ **指派：GD-108** · status: open · 优先级: P2 · 类型: 剧本覆盖
> ui-brief 从验收剧本推导动作清单时只见 6 条（charge.rock/throw.rock/throw.scissors/throw.paper/charge.paper/smoke.use），GDD §14 声明 10 条——缺的 4 条（按 §14 对照补）无剧本步骤练到，将导致：①UI 设计需求单动作清单不全（设计稿会漏画按钮）②S4 剧本对账盖不满。修法：GD 按词表对齐律补剧本步骤（步骤名=真 UI 动作名）后重跑 `node scripts/ui-brief.mjs --game game108` 重出需求单。

### REQ-108-TOOL-01 · UI 需求单的动作清单**从验收剧本抓**，抓不全 · [2026-08-08] · status: open（工具债·报设计线）

- **病**：`docs/design/game108/ui-briefs/brief-2026-08-08.md` 自动生成的「② 全动作清单」只有 6 条，
  漏了 `charge.scissors` / `shard.pick` / `duel.next`。它的「数据源」列自己写着**验收剧本**——
  而 v2 剧本恰好只举了两手的蓄力例、没用过后两条。**从剧本反推词表 = 只能得到剧本恰好用过的那些。**
- **后果**：设计方按这份单子做，屏上就会缺三个可点元素；而单子自带的「缺项亮 ⚠」自检也查不出来
  ——它比对的就是这份残缺清单本身。
- **同形事故第三次**：`ui-inventory` 量「够不够得着」而不是「点下去有人管」· 验收剧本照实现口径写 ·
  这次照剧本抓词表。**共同点：尺子照着被测物做。**
- **修法**：动作清单改从**词表单一真相**生成（本作 = `games/game108/theme.ts` 的 `ACT`；
  通用做法 = 每游戏声明一处 `ACT` 并由工具读它），剧本只作交叉校验：
  **剧本用到但词表没有 = 剧本错；词表有但剧本没覆盖 = 剧本漏测**——两个方向都该报。
- **另**：该单子还把 `duel.next` 标成「尚未接线」——**已过期**（2026-08-07 修好，试玩 5 条断言钉着）。
  单子应从**当前代码**核实接线状态，不从旧自检文档抄。

---

## ⛔ REQ-108-罚血接线 · wontfix（主程 Lead session 回驳·2026-08-07）

**原诉求**：【R-108-04】罚血「欠债 → 真扣血」——全局条件成立时扣**指定一侧**的 hp。
GDD 原文登记为「卡在引擎缺口 `REQ-108-ENG-07`·等 owner 判 A/B」。

**两件事都不成立：**

**① 编号撞车**：`REQ-108-ENG-07` 已被「结算门下的 intent 生命周期」占用（owner 2026-08-07 判 A·已交付）。
**这个撞是我造成的**——我开那单时只 grep 了 `requests.md`，没扫 `gdd.md`，而罚血的编号只登记在 GDD 里。
教训：**开单编号前扫全套 `docs/design/<game>/*.md`，不只扫 requests。**

**② 举证经实查证伪 → 回驳，不开引擎单。**

> 原举证：「能读全局的写不到指定侧，能写指定侧的读不到全局。」

`t2-self-rule` **两头都有**：
- `SelfRule.whenGlobal` —— 按**全局 id** 求值的附加条件（REQ-F-035「实体自治但受全局相位约束」），与 `when` 取 AND
- `SelfRule.do[].kind='modify-resource'` —— **施于自身**；而引擎一实体一组件 ⇒ 打到的就是**该侧那份 hp**

**等价数据写法**（每侧各挂一份，纯数据·零引擎改动）：

```jsonc
// 挂在 p1 实体上
{ "type": "SelfRule",
  "whenGlobal": { "kind": "resource", "id": "p1.debt", "cmp": "gte", "value": 1 }, // 全局门：该不该罚
  "when":       { "kind": "flag", "id": "p1.penalty.tick", "equals": true },       // 周期节拍（见下方⚠）
  "do":         [{ "kind": "modify-resource", "value": -5 }],                      // 扣自己的 hp
  "once": true }
```

**⚠ 写数据的人必须知道的一条（我实测撞出来的）**：`once` 的 `armed` 复位**只看 `when`（自身），不看 `whenGlobal`**
——实现里 `whenGlobal` 为假是 `continue` **整条跳过**，源码注释明写「armed 不动：once 规则跨相位保持待发」。
⇒ **周期节拍必须放 `when` 里**（读自身那份 Flag，id 保持全局唯一 → `Effect{kind:'set-flag'}` 仍可按 id 置位）。
放进 `whenGlobal` 只会罚第一次就再也不复位——**我第一版探针就是这么写错的，跑出来才发现。**

**证明测试**（`src/skills/tier2/self-rule.test.ts`「全局条件 → 只扣指定一侧（罚血形态·回驳证明）」3 例）：
① 全局欠债 ≥1 → **p1 掉血 100→95、p2 纹丝不动**（举证说做不到的正是这一条）
② 欠债清零 → 罚血自动停 ③ 周期性：一个上升沿只罚一次、回落复位后再罚一次，全程另一侧不受影响

**你那边只差把这段数据接上去**——账（`p1.debt`）与演出（环心「已欠 -N」+ 红纱）你们已经做全了。

### ~~REQ-108-PE-01~~ · A 闸裸防御：DECIDE_GATE 定手窗单独撤除无测试咬合 · [2026-08-08] · 主程复查门实测带出（撤修验红 A 零红）→ **指派：PE-108** · status: **✅ 已交付（待主程升 S4）** · 优先级: P2 · 类型: 测试防线
> 实测：`const gate = flag(DECIDE_GATE)`→THROWING_GATE 后 60 测全绿——因账期推迟（B 闸）同路兜住，A 闸现为零覆盖裸防御；将来动账期时 A 即无人看守。修法：加结构点名测试（断言 master:throw:* 的 EventWhen 条件含 DECIDE_GATE 旗）或构造仅 A 可拦的中途变输入路径；测试须自证「改回 THROWING_GATE 即红」。

### ~~REQ-108-GD-02~~ · capability-plan 补「心态机」消费点 + 自证仪式随 v5 重跑 · [2026-08-08] · 主程复查门（S2 第4条/S4 第12条·施工方自首主程确认）→ **指派：GD/PE-108** · status: **✅ 已交付（待主程升 S2/S4）** · 优先级: P3 · 类型: 文档同步+自证
> ①capability-plan.md 补一行心态机（State/set-state/Effect.chance/EventWhen 组合）消费点；②self-check 对齐单+截图序列按 v5 现状重玩重截（现档为 v5 前）。完成后知会主程将 S2/S4 复查升 PASS。

---

## 两单交付回执（2026-08-08·交主程复验）

**REQ-108-PE-01（A 闸裸防御）** —— 主程判词成立，我先前那次"A 转红"的结论是**在 v5 重写之前**跑的、
之后失效了（教训：撤修验红的结论跟着代码走，不跟着记忆走）。行为上确实测不到：v5 把台账推迟到结算之后，
⑤ 定手读的四样输入在 T2 里没有一样动得了。**改测结构** —— 被保护的性质本来就是结构性的
「AI 的决策面只准开一拍」：断言 `master:read:*`/`master:throw:*` 必须含一拍窗旗、
**且不许含整段 T2 的 `THROWING_GATE`**，再加一条「两个窗口态必须是无条件立刻走的过渡态」
（防有人给它加 `after` 把窗口悄悄拉宽）。**三种拉宽方式各自撤修即红**（已实跑）：
① 出招挂回 `THROWING_GATE` ② 判读挂回 `THROWING_GATE` ③ 给 `lockIn2` 加 `after: 30`。
另补一条行为半：整个 T2 全程两扇窗必须是关的（玩家一路乱按蓄力骚扰）。

**REQ-108-GD-02（plan 补行 + 自证重跑）** —— ① `capability-plan.md` §2 补三行（心态机 `j1-state`+
`set-state`+`{kind:'state'}` / `Effect.chance` 表演骰 / `vsResource` 统计冠军），
并把已过期的 `t2-weighted-spawn` 那行划掉写明 v5 为何不用它。
② 自证仪式在 v5 现状重跑：`playthrough` 33/33、`spec-recursion` 12 条 0 裸奔、截图 12 张重截、
好玩三问按第五档大师重答（第 4 轮记录）。

**⚠ 重跑当场逮出一个真 bug，比两张单子都重要** ——
`MASTER_PATCHES` 把判定表**整环反转**（石吃布·布吃剪·剪吃石），而 v4/v5 的决策链一直按**标准表**
算克星 ⇒ 大师每次"反制"出的恰恰是自己表里的败手。整局 sim：**玩家一直出同一只手，大师 100:0
一滴血不掉地输光**。更阴的一半：⑥ 回顾也用标准表判胜负，**它每输一局读准度还 +1**（自我认知与战果
完全相反·零报错·所以四道门一道都没红）。已修 + 两条撤修即红的点名测试（判据从 `MASTER_PATCHES`
**现推**，不抄答案——抄了两处会一起错）。修完 sim 变成：对复读机的最优解「永远布」对大师**必输**，
诈唬流对大师能赢 —— 一套打法通吃被打断，正是【R-108-32】要的教学曲线。

**顺带修的两处尺子过期**（同"改了游戏没改尺子"那一类）：
`game108-playthrough.mjs` 一进门就找 `ui.start`，加载闸上线后当场找不到键、后面 16 条连锁假红；
`game108-spec-recursion.mjs` 两处锚点过期（AI 相位门改挂定手窗 / 「超时顺延」v3 已作废连锁删）。
### REQ-108-ART-01 · S6 账单占位符整改：全部台账行补成形 SVG 矢量图 · [2026-08-08] · owner 复盘 S6 账单（「都是占位符没有真实矢量图·不合理」）→ **指派：PE/PA-108** · status: open · 优先级: P2（S6 门槛） · 类型: 美术占位达标
> 新手册红线（art-pipeline.md「占位最低标准=成形矢量图」·owner 2026-08-08 立）：每台账行须有真实入游戏渲染的成形 SVG（牌面像牌面·图标像图标），灰块/字框不合法。整改：逐行补程序化成形 SVG（保号·后续风格锚到位逐行替真图），重出 S6 账单给 owner 逐行可见图。

### REQ-108-ART-02 · index.json 用非规范 `key` 字段致换肤机制静默失活 · [2026-08-08] · ARTTOOL 双修顺带发现（主程路由）→ **指派：PE-108** · status: open · 优先级: P3（现被硬编码回退掩住·无表症） · 类型: 数据规范
> `public/games/game108/art/index.json` 条目用 `key` 而资产校验器与换肤机制按 `id` 取——三张手型图标的 skinKey 覆盖通道**静默从未生效**，目前恰被 `duel-screen.ts` 硬编码回退指向同文件掩住。修法：字段改 `id`（或补双写）+ 删/降硬编码回退为兜底注释；验收=创作台换一张手型图真上画面。

### ~~REQ-108-DOC-01~~ · 回填「对手 AI 数据化」生产线手册 · [2026-08-08] · 主程复盘指派 → 原指派 108 施工方·**owner 2026-08-10 改派手册撰写 session** · status: **✅ done（2026-08-10·同本提交交付）** · 类型: 手册回填
> 交付：`docs/playbooks/opponent-ai.md`（49 行·≤100 行规格·工单点名六件 + 判定表同源/一级一拍共八坑逐条带出处）+ `playbooks/index.md` 挂行 + 手册头承接 owner 2026-08-10「有 AI 必须有 AI 设定」新规（capability-plan §4.65 / S4 清单同口径）。反面教材（赖皮事故/静默旗/冗余盲区/判定表 891f33d89）在手册 §②④。
