# ⛔ 作废 · UI 设计需求单 · 拳律 Rule of Three（game108）

> **2026-08-08 作废（Lead）。这份是工具按 v1 旧真相自动生成的，三处已与现状不符——
> Claude Design 收到后判成「需求完全变了」，正是被这份误导。别按它做。**
>
> **现在的真相在这三处**：
> - **美术方向** → `games/game108/design_handoff_rule_of_three_battle/`（Claude Design 自己的定稿·**已 1:1 落地在跑**）
> - **玩法与节奏** → `docs/design/game108/gdd.md` §3 / §3b / §5b（v3）
> - **这次真正要设计的四件** → `docs/design/game108/ui-v3-change-brief.md`
>
> ## 逐条说明它错在哪（也是工具要修的地方）
>
> **① 品味槽写「走暗黑帝国感·金属质感强」、风格锚九个候选一个没勾** —— **过期，不是新方向。**
> 那句是 v1 立项时的意向（`brief.md` 同期还写着「暗调拳馆」）。后来 Claude Design 交的稿是
> **明亮卡通 + 青草地 + 奶油面 + 墨描边**，owner 认可并已逐像素落地（真渲染
> `public/games/game108/probe/S4-play-*.png`）。**美术方向以已交付的定稿为准，不翻。**
> 九个候选里最接近的是 `cartoon-thick`（厚描边卡通），但本作**已有专属定稿**，不必再挂风格包。
>
> **② 屏清单 6 张** —— 那是 GDD §13 的**长期规划**，不是当前工单。
> 现在**只有 S-03 对局屏落地**。S-04 二选一确实一张稿都没有、`shard.pick` 也确实没接线
> （`self-check/S4-alignment.md` 明记为 ❌），但那是**另一个阶段的活**，不该混进这次的节奏增量里。
>
> **③ 全动作清单漏了三条** —— 这是**工具的缺陷**，不是笔误：
> 它的「数据源」列写着**验收剧本**——而 v2 的剧本里恰好没有 `charge.scissors`（三手只举了两例）、
> 没有 `shard.pick`、没有 `duel.next`。**从剧本反推词表，就只能得到剧本恰好用过的那些。**
> 词表的单一真相是 `games/game108/theme.ts` 的 `ACT`：
> `charge.{rock,paper,scissors}` · `throw.{rock,paper,scissors}` · `smoke.use` · `shard.pick` · `duel.next`
> —— **一共 9 条，按全表落。**
> 另：`duel.next`「尚未接线」的说法**也已过期**——2026-08-07 已修（终局「再来一局」原是死键），
> 现在真能重开，试玩脚本有 5 条断言钉着。当前真正没接线的只剩 `shard.pick` 一条。
>
> **根因（与本仓两次同形事故一致）**：把「尺子」照着被测物做——
> 动作清单照剧本抓，剧本又照实现写，于是漏的地方三方一致、永远发现不了。
> 已开单 `REQ-108-TOOL-01`（下面）要求需求单改从**词表单一真相**生成。

---

<details><summary>以下为作废原文（留档对照·勿据此施工）</summary>

生成时间：2026-08-08

一句话玩法（pitch）：六条蓄力槽公开摊在台面上——你往哪一手存力所有人都看得见，然后你可以出别的。T1 蓄力(公开) → T2 出招(隐藏·同时) → T3 对决：克制关系定胜负、蓄力只决定赢了打多少(10/20/30/40)，出过的手清零。满蓄被克 = 一次赔掉三回合。判定表本身是数据，遗物能当场凿改它。超休闲操作 × 轻 roguelite 深度。

## ① 屏清单
- S-01 主菜单
- S-02 爬塔地图
- S-03 对局屏
- S-04 二选一
- S-05 结算
- S-06 图鉴
（来源：GDD「屏幕」一节）

## ② 全动作清单
| 动作名 | 触发语义 | 数据源 |
|---|---|---|
| `charge.rock` | 给该手 +1 蓄力（已满则该键禁用） | 验收剧本 |
| `throw.rock` | 出该手 | 验收剧本 |
| `throw.scissors` | 出该手 | 验收剧本 |
| `throw.paper` | 出该手 | 验收剧本 |
| `charge.paper` | 给该手 +1 蓄力（已满则该键禁用） | 验收剧本 |
| `smoke.use` | 放烟雾（每场 1 次） | 验收剧本 |

> 每一条都要在设计稿里真落一个可点击元素，`data-action` 原样照抄本表「动作名」列（不改字、不加前缀）。

## ③ 风格锚 + 品味槽
【该游戏暂未锚定风格包——从下列任选其一写进视觉方向，或直接描述参考图/关键词/情绪板】
· neon-synthwave（霓虹合成波）
· pixel-retro（像素复古）
· cartoon-thick（厚描边卡通）
· disney-supercell（迪斯尼×Supercell）
· dreamy-pastel（梦幻少女（暖暖系））
· apollo-toon（阿波罗卡通水墨（house style））
· sakura-nijigen（樱色二次元（女性向人物锚））
· vegas-victoriana（维多利亚赌宴（game-c 场景锚））
· modern-manor（现代豪宅夜局（game-a 场景锚））

品味槽（owner 一句话）：走暗黑帝国感·金属质感强

## ④ 输出契约（固定模板·不可删改）
- 单文件自包含 .dc.html；图片较多时打包为 zip（html + 资源文件，Claude Design 默认导出即为 zip）——两种形式都收，禁外链 `<script src=`。
- 每个交互元素必须带 `data-action="<②清单里的动作名>"`（照抄，不改字、不加前缀）。
- 标注屏尺寸（如 1920×1080 横版 / 456×788 竖屏）。
- 画出关键状态：正常 / 悬停 / 禁用 / 按游戏追加胜负态等。
- 完成后经工坊「🎨 设计稿产线 · 📥 收稿箱」交付。

## ⑤ 交付方式
两条路都可以（交付物是整包 zip 或单 .dc.html，两种都收）：
① **工坊拖拽**：回本工坊「🎨 设计稿产线 · 📥 收稿箱」，把 Claude Design 导出的 .zip（或单 .dc.html）整包拖进去/选择上传——自动落 `docs/design/game108/`（zip 落 `ui-refs/<稿名>/` 并保留包内相对路径结构）并登记台账（draft）+ 自动核对②动作清单（缺项亮 ⚠ 警示·不拒收）。
② **命令行**：不想开浏览器就 `python3 zerocraft.py design-import <zip路径> --game game108`——本机直接导入（同一套安全解包 + 登记 + 对账逻辑，零 HTTP）。
review 通过后勾/传「☑ 定稿」（定稿 = 1:1 复刻基准挂载对象）。

</details>
