// GD-103 验收剧本 ② · 升级触发：经验累计过 LEVEL_XP=5 → 等级+1（gdd §五/§八）。
//   30s(1800 tick) 足够累计 ≥5 经验 → 等级至少到 2（升级=三选一 draft 入口）。
// 对抗目标（改坏被测逻辑应变红）：
//   · 经验满不记等级 / 不扣阈值 → level 断言红。
//   · 升级阈值口径错（LEVEL_XP 变大到 30s 达不到）→ level 断言红。
{
  "name": "升级·经验过阈等级推进",
  "game": "game-103",
  "seed": 7,
  "steps": [
    { "expect": [ { "res": "level", "eq": 1 } ] },
    { "tick": 1800 },
    { "expect": [
      { "res": "level", "gte": 2 },
      { "sv": "status", "eq": "playing" },
    ] },
  ],
}
