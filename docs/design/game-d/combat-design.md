# Game D《骰途》· 战斗系统 + 呈现 · 活文档（迭代中）

> **配套**：`gdd.md`（GDD 管高层愿景）。**本文管战斗细节 + 呈现/关卡流式的迭代。**
> **方法论**：**结构锁定、数值/内容慢慢怼**（owner 2026-06-29「蛮复杂的系统，后面慢慢怼」）。复杂战斗在纸上定不出最优解——**锁骨架 → 搭原型 → 真的玩 → 边玩边调**。
> **状态**：🟡 骨架与 owner 共创中；数值/内容待原型 playtest 调。

---

## 1. 设计纪律（最高优先 · 加任何复杂度都不许破这几条）

1. **深度藏系统、决策摆明面（readability 铁律）**：系统可以六层乘算，但玩家每回合的决策必须**2 秒看懂**。敌人**属性/弱点/抗性/本回合攻击全明牌**（图标+颜色+大白话）；新手期直接提示"水系骰在这有效"。
2. **对随机的掌控权（防挫败）**：需要特定颜色却掷不出 = 最易让随机变挫败。**重掷/锁定/变色/商店买对色骰**必须够——前两小关就是攒"对色掌控力"的。
3. **结构锁、内容迭代**：骨架（攻防两层、四层乘算、骰子轴）定了别推翻；档位/数值/清单靠玩调。
4. **复杂度 = 数据，不是新引擎能力**：骰属性/抗性/攻防分配全是数据。系统再复杂，引擎不变，只是数据更丰富（数据驱动宣言）。

---

## 2. 骰子模型

- **一颗骰 = 6 面，每面 =（数字, 颜色）**。颜色 = 元素。
- **骰子库** = 通用骰（彩虹·6 面 6 色）+ 特制骰。
- **骰子属性轴**（任何骰 = 这几根轴上一个点·纯数据 schema·往里塞无穷骰也不乱）：
  | 轴 | 取值 | 例 |
  |---|---|---|
  | 颜色分布 | 彩虹 / 单色 / 半半 / 加权 | "某属性骰" |
  | 数值分布 | 标准 / 高滚 / 低而稳 | "点数小" / "点数大" |
  | 代价 | 免费 / 损耗 / 一次性 / 蓄力 | "消耗性很高" |
  | 特效（可选） | 附带小规则（掷 6 再掷…） | 稀有花头 |
- **构筑张力**：彩虹骰=灵活难凑大同色；单色专精骰=押注一元素换大倍率但僵硬。

---

## 3. 骰斗回合骨架（结构锁 · 攻防两层挂在第 3 步）

1. **明牌**：敌人亮出 属性 / 弱点 / 抗性 / 本回合攻击（元素+威力）。
2. **掷 & 筛**：两人掷骰池，锁定想要的、重掷其余，组装（快艇骰式）。
3. **分配（攻/防两池）**：
   - **攻击池** → 砸敌人血：`伤害 = Σ点数 × 颜色组合倍率 × 弱点系数 × 小丑修正`
   - **防御池** → 挡敌人本回合攻击：对的属性挡最有效；没挡住的扣**队伍生命**
   - **核心张力**：骰子永远不够两边分 → 速杀(全压攻击·硬吃伤害) vs 稳扎(先防·慢磨)；两人分工谁攻谁防。
4. **结算**：敌人掉血、你掉没挡住的血、套小丑/道具、判胜负。
- **退化基线**：最简单 = 点数直接扣血（你说的地板逻辑永远成立）。

---

## 4. 伤害公式骨架（数值档位待 playtest）

```
最终伤害 = (Σ 投入点数)            ← 加法地板
         × 颜色组合倍率            ← 3同色×2 / 5同色×3 / 6同色满色(1-6)×5  [档位待调]
         × 敌人弱点系数            ← 弱点×2 / 抗性×0.5或仅加法 / 免疫→挡掉
         × 小丑修正                ← buff 层（order 待定）
```
- **防御侧**：对应属性的 block 值，元素匹配增效（公式待怼）。

---

## 5. 敌人模型

- **元素类型** + 弱点 / 抗性 / 免疫（克制关系）。
- **HP** + **属性护甲**（"抵前 X 点非弱点伤害""对红色免疫"→ 逼换元素）。
- **本回合攻击**（元素 + 威力）→ 玩家要防。
- **Boss 刁难**（专属）：诅咒你的骰、要求伤害奇偶、抗性每轮轮换…→ 让你"很难掷出正确过关组合"。

---

## 6. 局结构：一层 = 3 小关（owner 拍）

- **小关 1、2（备料）**：打小兵 → 掉素材 / 选择 / 合成 / 预准备；**（建议）侦察 Boss 属性** → 针对性囤克制色骰/buff。
- **小关 3（Boss）**：属性 + 刁难拉满，逼你用前两关备好的料才打得过。
- **弧线**：备料 → 构筑 → Boss 检验（"我准备对了！"的爽）。小丑层照旧叠在最上面。

---

## 7. 呈现 / 关卡流式（owner 2026-06-29 加 · 技术对齐）

- **视图**：固定 **45° 等距(ortho)正视角**，随队伍前进**推进(forward dolly)**。= `Camera3D` 数据配置（ortho + follow + 固定角·**均已支持**）·**render-only 出 hash**。
- **关卡流式**：房间逐个流式加载，**线性前进、不回头**。**关键简化**：房间间的"抽 buff / 商店"**过场 = 天然加载边界**（不需无缝开放世界流式·只需"过场时加载下一房"）。
- **"永不停步" vs 回合制的调和**：宏观永远向前（过场时相机前推 + 流入下一房）；**战斗时相机定在当前房**（回合制掷骰静止）。"永不停步" = 主题/视觉贯穿（线性不回头）。
  - ⬜ **待 owner 确认**：战斗中相机是否也要微推进（"传送带"感）？还是战斗静止、仅过场推进（默认）。
- **确定性切分**：房间序列（哪个房·里面什么）= 确定性 sim（GameFlow + 种子）；前推/加载/卸载 = 纯表现。
- **实现两条路（M0 验证哪条够用）**：
  - **(A) 房间 = prefab 实例**：前方 spawn、身后 destroy（引擎已有 Prefab/spawn/destroy·**可能纯现成能力够**→ 全数据·不报主程）。**赌 A 够。**
  - **(B) assembly 层蓝图 load/unload seam**（**主程域**·若 A 不够才报评审）。

---

## 8. 域边界（谁做什么）

- **P3D（我）**：45° 相机 / 前推 / 房间渲染（render-only） + 数据蓝图 + `LayoutNode` UI + 3D 骰子翻滚表现。
- **主程**：流式 load/unload seam（若 prefab 不够） + **存档持久层** + 骰组件（若需新数据原语） + 新结算算子。

---

## 9. 待怼清单（慢慢磨 · 优先级粗排）

- [ ] 元素集 & 克制环定稿（提案 6 色：🔴火 🔵水 🟢木 🟡雷 ⚪风 🟣暗；环状相克）
- [ ] 颜色组合倍率档位 + 与小丑层的乘算 order
- [ ] 攻防分配规则（各分几颗？固定配额还是自由？）
- [ ] 防御的元素 block 公式
- [ ] 骰子代价机制（损耗怎么补充/恢复？）
- [ ] 首批特制骰清单（~8 种）
- [ ] 首批敌人/Boss 清单 + 刁难手法库
- [ ] 流式实现路径 A/B 验证（M0）
- [ ] 单人模式骰池规模（一池放大 or 操作双池）
- [ ] 战斗相机是否微推进（owner 确认）

---

## 10. 战斗 UI + 骰库经济 + 一局流程（owner 2026-06-29 设想）

### 10.1 一场战斗流程
1. 进房 → **敌人登场**（信息卡：血 / 元素 / 弱点 / 抗性 / 本回合威胁·全明牌）。
2. 每回合：① 两人从各自**骰库**选骰 → ② 掷（3D 翻滚进竞技场）→ ③ 锁定 + 重掷 → ④ 分配攻/防、凑颜色组合 → ⑤ 确定结算（砸敌血 / 挡威胁）。
3. 敌人血空 → **过关三选一奖励**（新骰 / 强化骰 / buff / 道具）→ 进骰库。
4. 往上一间。

### 10.2 骰库经济（"骰子口袋"怎么长大）
- **基础骰**：开局每人若干。
- **过关补充**：每清一间补几颗。
- **强化骰**：过关三选一里挑（升级 / 特制）。
- **局外带入的特殊骰**：跑局前从 meta(命运祭坛) 装备；**可带功能**（不只伤害——重掷 / 变色 / 护盾 / 抽骰 / 导能… 等 utility）。
- → **骰库 = 这局的 build 载体**（同 game-e 牌组心智·骰子是数据·随机 + 组合涌现）。

### 10.3 底 UI（详见 `ui-brief.md` 屏C）
两名玩家的骰库底栏（左A右B·从口袋选骰"怎么用"）+ 选骰/掷/重掷/攻防/道具控制 + 敌人区(信息明牌) + 边角 HUD。

---

> **下一步**：M0 核验（流式 A/B、存档、骰表达、game-e 复用度）→ M1 单人垂直切片用**最小内容**把骨架跑起来 → **靠玩调数值/内容**，本文持续更新。

---

## 11. 双人协作机制（owner 2026-06-29：怎么"互相补全 + 同时扔凑出组合"）

核心：双人时两人的骰**合并成一次共享攻击**，协作红利来自三处——

### 11.1 合池补全（"我没认出/没有的，你来补"）
- 敌人弱某色。A 这把没掷出/没带那色 → **合池 = A骰 + B骰，主元素 m 跨两人一起数**，B 来补。
- 例：A 有 2 火、B 有 2 火 → 合起来 4 火（×2.2），谁单独都凑不到。
- 设计取向：**鼓励两人点不同色亲和**（A 火/水、B 金/木）→ 任何敌人弱点色，总有一人手里有 → 天然"你补我缺"。

### 11.2 同时扔·凑出组合（"两人正好凑出什么"）
- **同点共鸣（连珠）**：A、B 掷出相同点数 → 共鸣加成（每对同点 +固定伤害 / +mult·连珠骰强化）。"正好凑出"的爽点。
- **跨人元素组合**：m 跨两人合数 → 比单人更易上高 mult 档。
- **接力/传递**：A 出 setup（导能染色 / 增幅）→ B 的攻击吃加成；或 A 传一颗骰给 B（传递骰）。

### 11.3 分工 + 互救
- 一人主攻（攻击池）、一人主防（防御池挡威胁），或都喂共享攻击·按回合分工。
- A 掷崩（杂色/低点）→ B 用 重掷石/导能骰 修 A 的骰、或合池盖过烂手 → **容错来自两人兜底**。

### 11.4 为何双人敌更强但不更难
- 合池 + 共鸣 + 互补色 + 互救 = 显著协作红利 → 故敌 HP×1.9、BOSS 每回合漂移弱点（逼跨人补色）。
- ⚠️ 模拟器**尚未建模**这些红利 → 双人在模拟里被低估（偏难）；补模型 / 实玩校。
- 落地：单人原型(M1)先跑通；M2 扩成「合池攻击 + 同点共鸣 + 互补色 + 互救件」。
