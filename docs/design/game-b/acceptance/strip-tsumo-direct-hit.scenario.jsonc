// 查的规则：直击制·自摸分支——被自摸支付的三家各脱 1，主角付家豁免、和牌者不脱（gdd §三/§七）。
//   ⚖ 自摸=支付三家各脱一件（gdd §三 owner 2026-07-17「谁输谁脱」确认）：自摸时三个付家各被直击脱 1；
//     和牌者自己不脱；付家里若含主角则主角豁免（§三 主角不脱·只扣分）。
//   seed=7 定盘：莉世(席2)自摸 → 付家=主角(席0)/绫(席1)/小夜(席3)；主角豁免 → 绫、小夜各脱 1，主角仍 5，莉世(winner)仍 5。
//   规则依据：gdd §三「被自摸=支付三家各脱一件」「主角不脱」、§七「被自摸支付的三家中姨太各脱 1…主角豁免」。
// 对抗目标：
//   · 自摸误让和牌者也脱 → clothing_2(莉世,winner) < 5 照出。
//   · 主角作为付家没豁免（symmetric 默认应 false）→ hero_clothing < 5 照出。
//   · 三付家里漏脱某家（只脱一家/只脱最多支付者）→ clothing_1(绫) 或 clothing_3(小夜) = 5 照出。
//   · 一次直击脱多件 → clothing_sum < 18 照出。
//   · 结算破坏点数守恒 → score_sum ≠ 200000 照出。
{
  "name": "直击脱衣·自摸（三付家各脱1·主角豁免）",
  "game": "game-b",
  "seed": 7,
  "config": { "seatNames": ["主角", "绫", "莉世", "小夜"] },
  "steps": [
    { "signal": "play-round" },
    {
      // 自摸收束：莉世(席2)和牌；付家绫/小夜各脱 1，主角付家豁免(=5)，和牌者莉世不脱(=5)。
      "expect": [
        { "flag": "win_end", "eq": true },
        { "sv": "result_type", "eq": "tsumo" },
        { "sv": "winner_name", "eq": "莉世" },
        { "sv": "loser_name", "eq": "none" },
        { "res": "hero_clothing", "eq": 5 },
        { "res": "clothing_1", "eq": 4 },
        { "res": "clothing_2", "eq": 5 },
        { "res": "clothing_3", "eq": 4 },
        { "res": "clothing_sum", "eq": 18 },
        { "res": "score_sum", "eq": 200000 }
      ]
    }
  ]
}
