// 查的规则：立直全生命周期——中局宣立直（扣 1000 进供托·台面点数搬移）→ 终局供托回收守恒（gdd §四/§十二）。
//   立直棒 1000（gdd §四）：宣立直者从自家点数扣 1000，置于台面成供托 → 该家 score-1000、kyotaku+1000；
//     这是点数**搬移**不是新增，故此刻台面四家点数和 = 200000-1000 = 199000（供托暂离台面点数）。
//   供托归属（gdd §四）：立直棒随和了归和者 / 流局滚存 / 终局残供托归第 1 位 → over 时 kyotaku 必 = 0，
//     四家点数和守恒回 200000（立直棒既没丢也没凭空增）。
//   seed=17 定盘：第 18 步 AI 时绫(席1)听牌宣立直 → riichi_1=true、kyotaku=1000、score_1=49000、台面和 199000、局仍在进行。
//   规则依据：gdd §四 立直棒 1000 / 供托归属；§十二 确定性（同 seed 同轨）。
// 对抗目标（把立直棒记账写错的程序员会犯的错）：
//   · 立直没扣点/扣了没入供托 → @18 score_1 ≠ 49000 或 kyotaku ≠ 1000 照出。
//   · 把供托也算进台面点数（重复计）→ @18 score_sum ≠ 199000 照出。
//   · 终局漏回收残供托 → over 后 kyotaku > 0（且 score_sum < 200000）照出。
//   · 立直棒回收两次/凭空生点 → 终局 score_sum ≠ 200000 照出。
{
  "name": "立直全生命周期（扣1000→供托→回收守恒）",
  "game": "game-b",
  "seed": 17,
  "config": { "seatNames": ["主角", "绫", "莉世", "小夜"] },
  "steps": [
    { "tick": 18 },
    {
      // 中局态：绫(席1)已立直，1000 从其点数扣出成供托；台面四家点数和 = 199000（供托暂离台面）。
      "expect": [
        { "sv": "phase", "eq": "playing" },
        { "flag": "riichi_1", "eq": true },
        { "res": "kyotaku", "eq": 1000 },
        { "res": "score_1", "eq": 49000 },
        { "res": "score_sum", "eq": 199000 },
        { "flag": "over", "eq": false }
      ]
    },
    { "signal": "play-match" },
    {
      // 终局：残供托已回收（台面清零），立直棒回到点数流转 → 四家点数和守恒回 200000。
      "expect": [
        { "flag": "over", "eq": true },
        { "res": "kyotaku", "eq": 0 },
        { "res": "score_sum", "eq": 200000 }
      ]
    }
  ]
}
