// 查的规则：摸打节奏——每步恰一摸一打·座位顺时针轮转·牌山匀速递减（gdd §三 每局摸打循环·§十二 确定性）。
//   日麻回合节奏（gdd §三「每局：摸打/鸣牌/立直」）：每个行动步 = 当前家打一张 → 下家摸一张；
//     故每推进一步：turn 顺时针 +1（(turn+1)%4，逆座风东→南→西→北的行动序），活山 -1（下家摸牌）。
//   开局庄家(席0)已首摸（活山 70-1=69）；此后 tick k 步 → turn=k%4、wall=69-k（匀速直到有人和了/流局）。
//   一圈四步（t1..t4）后 turn 回到起家席0、活山连减 4 = 65——节奏可预测、不停滞、不跳步。
//   规则依据：gdd §三 核心循环摸打；§四 四人局；§十二 同 seed 同轨（步步确定）。
// 对抗目标（把回合推进/摸牌节奏写错的程序员会犯的错）：
//   · 打牌后没轮转下家（turn 卡住）→ t1 turn ≠ 1 照出。
//   · 轮转方向错/跳席（off-by-one 或逆向）→ t2/t3 turn 序列不符照出。
//   · 一步摸多张或漏摸（节奏错乱 / 牌山透支）→ wall 不按 -1 匀速递减照出。
//   · 一圈后没回到起家（模 4 错）→ t4 turn ≠ 0 照出。
{
  "name": "摸打节奏（顺时针轮转·牌山匀速递减）",
  "game": "game-b",
  "seed": 2,
  "config": { "seatNames": ["主角", "绫", "莉世", "小夜"] },
  "steps": [
    {
      // 开局：庄家席0 首摸后待打，活山 69。
      "expect": [
        { "sv": "phase", "eq": "playing" },
        { "res": "turn", "eq": 0 },
        { "res": "wall", "eq": 69 }
      ]
    },
    { "tick": 1 },
    { "expect": [ { "res": "turn", "eq": 1 }, { "res": "wall", "eq": 68 } ] }, // 席0 打→席1 摸
    { "tick": 1 },
    { "expect": [ { "res": "turn", "eq": 2 }, { "res": "wall", "eq": 67 } ] }, // 席1 打→席2 摸
    { "tick": 1 },
    { "expect": [ { "res": "turn", "eq": 3 }, { "res": "wall", "eq": 66 } ] }, // 席2 打→席3 摸
    { "tick": 1 },
    { "expect": [ { "res": "turn", "eq": 0 }, { "res": "wall", "eq": 65 } ] }, // 席3 打→回起家席0（一圈四步·牌山-4）
    { "tick": 1 },
    {
      // 第五步：节奏继续（turn=1、wall=64），全程仍在进行中（无停滞）。
      "expect": [
        { "res": "turn", "eq": 1 },
        { "res": "wall", "eq": 64 },
        { "sv": "phase", "eq": "playing" }
      ]
    }
  ]
}
