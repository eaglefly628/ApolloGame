// 查的规则：荒牌流局结算逻辑——无和牌·听牌罚符点移零和·立直棒入供托滚存·流局不脱衣·台面守恒自洽。
//   荒牌流局（gdd §四 流局行「荒牌流局+听牌罚符 3000 分配」）：牌山摸尽无人和了 → 相位=draw、无和牌牌；
//     听牌罚符 3000 在听牌/不听两组间转移（gdd §四）——**点移严格零和**（delta Σ=0·不凭空生灭点）。
//   立直棒（gdd §四 立直棒 1000·供托归属「流局滚存下局」）：本局立直者的 1000 已入供托，流局不结算给谁、
//     滚存下局 → kyotaku 保持 = 立直棒数×1000；台面四家点数和 = 200000 - 供托（点数搬移·守恒自洽）。
//   脱衣仅直击触发（gdd §七「立直棒/罚符/流局不触发」）→ 流局 clothing_sum 恒 = 20。
//   seed=10 定盘：荒牌流局·1 家听牌立直·罚符按 3000 在 1听/3不听间分配·1 根立直棒滚存（kyotaku=1000）。
//   规则依据：gdd §四 荒牌流局/听牌罚符 3000/立直棒 1000/供托流局滚存；§七 流局不脱衣；§十二 确定性守恒。
// 对抗目标（把流局/供托记账写错的程序员会犯的错）：
//   · 流局误置和牌（win_tile≥0 或 phase≠draw）→ 照出。
//   · 听牌罚符点移非零和（收付不等）→ delta_sum ≠ 0 照出。
//   · 立直棒流局漏滚存/误发/丢失 → kyotaku ≠ 1000 或 score_sum ≠ 199000 照出。
//   · 把供托也算进台面点数（重复计）→ score_sum ≠ 199000 照出。
//   · 流局误触发脱衣（罚符当直击）→ clothing_sum < 20 照出。
{
  "name": "荒牌流局结算逻辑（罚符零和·立直棒滚存·不脱衣）",
  "game": "game-b",
  "seed": 10,
  "config": { "seatNames": ["主角", "绫", "莉世", "小夜"] },
  "steps": [
    { "signal": "play-round" },
    {
      // 荒牌流局收束：无和牌、罚符点移零和、1 根立直棒滚存供托、台面守恒自洽、流局不脱衣。
      "expect": [
        { "flag": "win_end", "eq": true },
        { "sv": "result_type", "eq": "draw" },
        { "sv": "phase", "eq": "draw" },
        { "sv": "winner_name", "eq": "none" },
        { "res": "win_tile", "eq": -1 },
        { "res": "delta_sum", "eq": 0 },
        { "res": "tenpai_count", "eq": 1 },
        { "res": "kyotaku", "eq": 1000 },
        { "res": "score_sum", "eq": 199000 },
        { "res": "clothing_sum", "eq": 20 }
      ]
    }
  ]
}
