# 操作手册 · DokiWorld 卡带导出（**已退役** 2026-08-13）

> **本线已整体退役**（owner「老的不需要了·只要一个 DokiWorld 产出」）：这是官方 App SDK 规范落地**之前**的旧交付形态
> （协议桥 v1 · iframe 卡带 · 仅 a/b/c），对方现按新规范收包，旧产物不再被接收。发布屏按钮与服务端分支已同日撤除。
>
> **现行唯一出包线 → `docs/playbooks/dokiworld-pack.md`**（官方 SDK App 包·manifest/生命周期/结算/挂起恢复全套）；
> 对方协议 = `docs/design/dokiworld/app-sdk-app-development.zh-CN.md`（规范快照）· SDK 能调什么 = `docs/design/dokiworld/sdk-surface-2.1.0.md`。
> 本手册旧全文与导出插件（`tools/export-targets/dokiworld.mjs`）查 git 历史；a/b/c 要上 DokiWorld 也走新线（照手册建 `dokiworld/<slug>/`）。
