{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '[核心规则·每条新需求必先执行] ① data-driven-manifesto 是最高纲领（游戏=数据，代码只属引擎确定性解释器）。② 对 PA/PB/PC 的每一条需求，先以资深程序员+架构师视角评判该不该做：能用现有 capability 组合 / 已被现有能力覆盖的 → 带全部理由回驳，不盲从；真表达不了的缺口才下沉成通用 capability；游戏专属代码/手写 UI 倾向消解为数据。③ 把接受 or 回驳的结论 + 全部理由报告给用户，让用户知情决策。详见 CLAUDE.md。'"
          }
        ]
      }
    ]
  }
}
