node_modules/
dist/
.env
*.log
.DS_Store
.apollo/registry.json
.claude/worktrees/
__pycache__/
*.pyc
# Gemini review 导出包是"生成→发一次"的临时产物，别入库占 token（历史包已删，见 git history）。
review-for-gemini*.txt
# 离线渲染帧（render-frame.ts 的可再生产物，按需生成发一次，不入库）。
game-d-frame.svg
