node_modules/
dist/
dist-cartridge/
platform-dist/
release/
apollo-*-rk3562.html
apollo-*-rk3562.tar.gz
public/games/*/cover.png
.env
*.log
.DS_Store
.apollo/registry.json
# 设计草稿·未定名（创作中间态持久化·用户数据·随会话产生，不入引擎仓；定名后随卡带落 library/ 亦已 gitignore）
.apollo/design-drafts/
# LLM 交互日志（每次生成往返落一行 JSONL·排障用·随会话产生·API key 绝不落盘·不入引擎仓）
.apollo/llm-logs/
# 工坊对话历史（每卡带每角色·工作台状态非游戏数据·owner 07-11 session 持久化）
.apollo/workshop-chats/
# 能力缺口台账（capgap 快速通道·运行时追加·下沉裁决以 requests.md 为准）
.apollo/cap-gaps.jsonl
# 素材库虚拟分组（工作台状态·收藏夹式引用·不动素材本体）
.apollo/matlib-groups.json
# 订阅通道子进程空 cwd（安全隔离目录·运行时产生）
.apollo/claude-code-cwd/
.claude/worktrees/
__pycache__/
*.pyc
# Gemini review 导出包是"生成→发一次"的临时产物，别入库占 token（历史包已删，见 git history）。
review-for-gemini*.txt
# 离线渲染帧（render-frame.ts 的可再生产物，按需生成发一次，不入库）。
game-d-frame.svg
game-e-preview.svg

# 会话临时自验产物（截图脚本/PNG·勿入库）
scratchpad/

# UI 审计工具临时构建产物
.ui-audit-tmp/

# ApolloBench 帧时 prior 存档（墙钟按机器/运行波动·delta 回归用·不入库）
bench-results/

# 创作台 v1 用户游戏库（用户数据·每游戏自成独立 git 仓/快照，不入引擎仓）
library/

# 创作台 v1 BYO-key 设置（用户自带 API key·明文本地存储，绝不入引擎仓）
.apollo-config.json

# 工坊自建命名风格预设库（owner 2026-07-22·本机自建风格·绝不入引擎仓）
.apollo-styles.json

# mock 试跑产物（owner 2026-07-10「Mock 数据不该这样做」：仅平台墙预览·永不写回/入库；
# 真图 gen/art-NN 是游戏观感一部分该入库，mock 命名空间 gen/mock/ 机械挡在仓外）
public/games/*/art/gen/mock/
.cache-fonts/
